# v0.1 Task Board - SaaS Core And Contracts

Version: `v0.1`

Goal: lock the SaaS foundation so all services share the same product model, access model, and integration contract.

Target date: 2026-06-28.

Current status: `In progress`

## Version Scope

- Confirm `market-signal-ai-bot` as the core account/subscription/access service.
- Confirm integration contract between `telegram_company_matcher_app` and `stock-signal-scanner`.
- Confirm internal access check before paid delivery.
- Prepare QA, DevOps, Legal, and documentation gates for the next versions.

## Yasha - Developer

### Tasks

- [x] Confirm current `market-signal-ai-bot` endpoints against README and docs.
- [x] Confirm `/api/internal/access` behavior with active, expired, and missing subscriptions.
- [x] Confirm SaaS account routes are implemented or mark missing routes clearly.
- [x] Confirm API key and analysis history tables are wired to actual endpoints.
- [x] Fix any mismatch between documented API and implemented API.
- [x] Keep `npm run check` and `npm test` passing.
- [x] Добавить безопасный split-secret slot для dev matcher key id `matcher-dev-v1`: Core читает `INTERNAL_API_SECRET_MATCHER_DEV_V1`, не перезаписывая общий keyring и не используя Bearer auth.
- [x] Реализовать website internal route/scope: `POST /api/internal/subscriptions` требует HMAC key id сайта и scope `website:subscriptions`.
- [x] Реализовать production endpoint `POST /api/internal/access/check`, через который `stock-signal-scanner` получает решение о доступе и списании квоты до запуска анализа.
- [x] Принимать и валидировать поля `contractVersion`, `requestId`, `userId`, `chatId`, `ticker`, `reportType`, `forceRefresh` и `language`; `reportType` допускает только `regular` или `fundrep`.
- [x] Возвращать поля `contractVersion`, `requestId`, `allowed`, `chargeUnits`, `quotaDecision`, `cacheStatus`, `reportSource`, `remainingUnits` и `reason` в соответствии с `docs/integration-contract.md`.
- [x] Добавить журнал квоты и идемпотентное решение по `requestId`: повтор того же запроса не должен повторно списывать единицы и должен возвращать ранее принятое решение.
- [x] Оставить billing, ownership и quota ledger только в `market-signal-ai-bot`; не принимать доверенные данные о тарифе, балансе или праве доступа из публичного payload.
- [x] Настроить безопасное поведение при ошибках: не разрешать анализ, если подписка, владелец или квота не подтверждены.
- [x] Добавить миграцию базы данных, автоматические тесты разрешения/отказа, исчерпанной квоты, повторного `requestId`, неверного payload и неподдерживаемой версии контракта.
- [x] Передать реализацию Роману на проверку безопасности и менеджеру `stock-signal-scanner` на интеграционную проверку.

### Новые задачи Яше после проверки Романа от 2026-06-29

Текущий порядок работы после закрытия email-P0:

1. Реализовать применение API-ключей и серверных прав на endpoint анализа: это оставшийся P0 без внешней зависимости.
2. Сделать HMAC обязательным и добавить защиту внутренних запросов от повторного воспроизведения: это необходимо для безопасной интеграции `stock-signal-scanner`.
3. Затем закрывать Google OAuth, админскую защиту и платёжные уведомления по мере получения внешних настроек и согласований.

Очередь P0 - блокирует любой публичный или платный запуск:

- [x] Закрыть захват аккаунта по email: `/api/register` не должен выдавать сессию существующего аккаунта только по совпадению неподтверждённого email.
- [x] Добавить подтверждение владения email одноразовой ссылкой или кодом; до подтверждения запрещено входить в существующий аккаунт или привязывать к нему новую личность.
- [ ] Реализовать вход через Google по Authorization Code + PKCE с серверными `state` и `nonce`, проверкой издателя, аудитории, срока действия, `email_verified` и разрешённого списка адресов возврата.
- [ ] Убрать `token` и `adminUser` из URL; внедрить короткие админские сессии, роли `viewer/operator/owner`, ограничение неудачных попыток входа, аудит и MFA либо согласованную Романом компенсирующую защиту.
- [ ] Сделать обработку платёжных уведомлений идемпотентной по идентификатору события провайдера; проверять аккаунт, среду, допустимые переходы статуса и запрещать выдачу доступа из тестовых, повторных или чужих событий.
- [x] Подключить созданные API-ключи к реальной авторизации анализа: серверный список разрешённых прав, проверка прав, лимиты по ключу, пользователю и IP, отзыв и ротация; усилить хеш серверным секретом.

Очередь P1 - обязательна до повторной проверки безопасности:

- [x] Запретить обход HMAC через общий Bearer-секрет; требовать подписанный запрос, `X-Request-Id`, защиту от повторного воспроизведения и идентификатор ключа для ротации.
- [ ] Для изменяющих состояние запросов с cookie дополнительно проверять `Origin`/`Referer` на тот же источник.
- [ ] Добавить 30-минутный период бездействия сессии, ограниченный абсолютный срок, выход с отзывом сессии и cookie с префиксом `__Host-` в рабочей среде.
- [ ] Сократить срок Telegram init data для входа и привязки аккаунта до 5-15 минут; 24 часа не использовать для установления личности.
- [ ] Добавить негативные тесты на захват аккаунта через email, межсайтовый CSRF, обход админки через URL, повтор платёжного уведомления, повтор или подмену внутренней подписи и обход прав API-ключа.
- [ ] После выполнения передать Роману код, миграции и результаты тестов на повторную проверку.

### P0 Production Access Endpoint - задание от scanner 2026-06-30

Решения главного менеджера от 2026-06-30:

- [x] Утверждена стоимость: regular new/refresh `1`, regular cached `0.5`, own repeat `0`; FundRep new/refresh `3`, FundRep cached `1.5`, own repeat `0`.
- [x] Утверждён cache TTL `60 минут`.
- [x] Новый расширенный контракт выпускается как `contractVersion: "1.1"`.
- [x] Scanner передаёт подписанный cache status; контракт должен включать данные, позволяющие проверить соблюдение TTL.
- [x] Внутренний журнал хранит целые минимальные полукредиты: `0.5 unit = 1 credit`; API возвращает `chargeUnits` в пользовательских единицах.

Коррекция после проверки Романа:

- [x] Scanner cache status остаётся только диагностической подсказкой и не влияет на стоимость.
- [x] Утверждена core-owned модель: скидка разрешается только по cache entry, созданной Core после одноразового cache receipt.
- [x] Добавить таблицы pending cache receipts и committed cache entries отдельной миграцией `0013`.
- [x] Для разрешённых `new_*`/`refresh_*` создавать Core-issued `cacheReceiptId` и возвращать тот же receipt при идемпотентном повторе без нового списания.
- [x] Реализовать HMAC-only `POST /api/internal/access/cache/commit`; принимать только существующий неиспользованный receipt с совпадающими неизменяемыми полями и digest результата.
- [x] Core самостоятельно задаёт время создания/истечения cache entry; access check рассчитывает скидку только по core-owned ledger и игнорирует scanner cache metadata для биллинга.
- [ ] Добавить реальный Workers-runtime/D1 тест конкурентных одинаковых запросов, однократного commit, фиктивного cache hit и истечения TTL.
- [x] Подготовить безопасную повторяемую dev-only фикстуру: тестовый пользователь, активная подписка и квота; без production-данных, публичного seed endpoint и секретов в репозитории.
- [ ] Передать исправление Роману и Лене; production остаётся заблокированным до их PASS.

Дополнительный security-gate перед передачей Роману:

- [x] Добавить endpoint-level service authorization: scanner key может вызывать только scanner access/cache endpoint-ы, matcher key - только matcher access/delivery endpoint-ы.
- [x] Не определять полномочия только по знанию общего HMAC-секрета или строковому префиксу key ID; использовать управляемую allowlist/scope map для активных key ID.
- [x] Добавить негативные тесты: matcher key отклоняется на `/api/internal/access/check` и `/api/internal/access/cache/commit`, scanner key отклоняется на `/api/internal/deliver`.

- [x] Не возвращать Bearer-обход: production endpoint должен использовать утверждённый HMAC-only контракт с отдельным scanner key id и уникальным request id.
- [x] Реализовать контракт `1.1` с `generationVersion` и подписанными cache status/cache age данными от scanner.
- [x] Реализовать решения `new_regular`, `own_repeat`, `cached_regular`, `refresh_regular`, `new_fundrep`, `own_repeat_fundrep`, `cached_fundrep`, `refresh_fundrep`, `rejected_no_quota`, `rejected_no_access`.
- [x] Добавить миграцию политики квот для полукредитов и TTL 60 минут, не меняя историю уже принятых решений.
- [x] Обеспечить идемпотентность по составному ключу `requestId + ticker`; определить поведение одного request id для нескольких тикеров.
- [x] Добавить контрактные тесты всех regular/FundRep/new/cached/refresh/repeat/no-quota/no-access сценариев и HTTP-политики.
- [x] Не выполнять production deploy до одобрения Романа, готовности секретов и синхронного обновления scanner.

### P0 Matcher Delivery Endpoint

- [x] Подтвердить key ID matcher: dev `matcher-dev-v1`; production `matcher-prod-v1` зарезервирован и не активируется до production approval.
- [x] Зафиксировать контракт `POST /api/internal/deliver` версии `1.0` в `docs/integration-contract.md`.
- [x] Яша: реализовать HMAC-only route, runtime validation, повторную проверку доступа, разрешение получателя через `userId`/`routeId`, Telegram send и audit.
- [x] Яша: добавить D1 delivery ledger с идемпотентностью по `deliveryId`; повтор не должен отправлять второе сообщение, изменённый payload должен отклоняться.
- [x] Яша: запретить raw Telegram chat ID, bot token, monitored-source metadata и неподдерживаемую разметку в payload.
- [x] Яша: добавить тесты sent/duplicate/access_denied/invalid/HMAC replay/Telegram failure и безопасного retry.
- [ ] Роман: проверить HMAC, arbitrary-recipient abuse, content injection, secret leakage и duplicate delivery.
- [ ] Лена: выполнить live dev delivery с test bot/route и проверить статусы, retry и отсутствие дублей.

### Report

Status: `Заблокировано проверкой безопасности; исправления P0/P1 назначены Яше`

Completed:

- Confirmed implemented Worker routes against README and integration docs.
- Confirmed SaaS account routes exist: `/api/me`, `/api/me/profile`, `/api/me/subscription`, `/api/me/api-keys`, `/api/me/analysis-history`.
- Confirmed API key and analysis history D1 tables are wired to endpoints via `api_keys`, `analysis_requests`, and `api_usage_daily`.
- Confirmed `/api/internal/access` behavior for active subscription, expired `current_period_end`, and missing subscription.
- Fixed documentation mismatch: README now includes `/api/health`, `/api/config`, and `/api/admin/bot-routes`.
- Fixed integration contract mismatch: documented `subscription_period_expired`.
- Added test coverage for missing-subscription internal access and expanded health table checks.
- Added Worker fetch smoke coverage for `/api/me/api-keys` -> `api_keys`.
- Added Worker fetch smoke coverage for `/api/me/analysis-history` -> `analysis_requests`.
- Verified `npm run check` passes.
- Verified `npm test` passes with 16 tests.
- Implemented production `POST /api/internal/access/check` with strict contract `1.0` request validation and fail-closed decisions.
- Added core-owned quota policies, balances, and immutable/idempotent decisions in D1 migration `0008_scanner_access_quota.sql`.
- Added atomic quota reservation: `regular` costs 1 unit and `fundrep` costs 5 units; seeded MVP period limits are `trial=25`, `starter=100`, and `pro=1000`.
- Added payload hashing so the same `requestId` replays its stored decision, while a changed payload with the same ID is rejected without another charge.
- Added POST HMAC body binding using SHA-256 of the raw body; aligned `/api/internal/analysis-requests` with the same protection.
- Added scanner access request/response TypeScript types and corrected the POST examples and HMAC format in `docs/integration-contract.md`.
- Extended `/api/health` with quota policy, balance, and decision tables.
- Verified `npm run check` passes and `npm test` passes with 24 tests.
- Verified all migrations through `0008_scanner_access_quota.sql` apply successfully to local D1 with Wrangler 4.96.0.
- Closed unverified-email account takeover: email-only `/api/register` no longer looks up an existing user, creates an account, or issues a session.
- Added `email_verified_at`; email lookup now resolves only verified addresses, and changing an email resets verification.
- Added migration `0009_verified_email_identity.sql` to revoke legacy email-only sessions without verified ownership.
- Added `0010_pending_email_identity.sql` so unverified addresses cannot reserve or squat a login identity; profile/admin email changes remain pending until confirmation.
- Session validation now rejects and revokes unverified email-only identities; verified Telegram sessions remain available without treating submitted email as verified.
- Subscription intake now fails closed instead of attaching or creating paid access by unverified email alone.
- Added regression tests for email-only registration takeover, legacy session rejection, Telegram email non-binding, and subscription attachment by unverified email.
- Verified `npm run check` passes and `npm test` passes with 27 tests.
- Verified migrations `0009_verified_email_identity.sql` and `0010_pending_email_identity.sql` apply successfully to local D1.
- Added authenticated `POST /api/analysis/requests` with `analysis:write` scope, active key/user/subscription checks, quota enforcement, and atomic per-key/user/IP limits.
- New API keys use HMAC-SHA256 with `API_KEY_HASH_SECRET`; legacy SHA-256 keys fail closed with `api_key_rotation_required` and can be revoked/replaced through account key management.
- Added atomic `POST /api/me/api-keys/rotate`: the previous key is revoked in the same D1 batch that creates its one-time replacement.
- API key scopes are restricted to the server allowlist; revoked, expired, wrong-scope, inactive-user, and inactive-subscription requests are rejected.
- Removed the internal Bearer-secret bypass. Internal requests now require HMAC, key ID, unique request ID, timestamp, canonical query, and raw-body digest.
- Added D1 replay ledger `internal_request_nonces`; duplicate `(keyId, requestId)` calls return `409 internal_request_replayed`.
- Added migration `0011_api_auth_and_internal_replay.sql`, health coverage, contract documentation, and negative security tests.
- Verified `npm run check` passes and `npm test` passes with 33 tests.
- Verified migration `0011_api_auth_and_internal_replay.sql` applies successfully to local D1.
- Implemented scanner access contract `1.1` with generation and signed cache metadata validation, 60-minute TTL, and force-refresh bypass.
- Added all approved regular/FundRep quota decisions and converted API `chargeUnits` from integer half-credit storage.
- Added `quota_decisions_v11` with composite `(request_id, ticker)` idempotency while preserving the complete `1.0` journal.
- One request ID may contain multiple independently charged tickers; changing immutable payload fields for the same pair returns HTTP `400`.
- Added migration `0012_access_contract_v11_half_credits.sql`; verified local D1 contains both old/new journals and credit balance columns.
- Added contract tests for regular/FundRep new, cached, refresh, own-repeat, no-quota, no-access, TTL 59/60/61, generation mismatch, force refresh, multi-ticker IDs, changed payload, and rejected replay.
- Added explicit CI production gates for Roman's security approval and successful scanner integration testing; push deploys dev only.
- Verified `npm run check` and the full test suite pass with 40 tests after contract `1.1` implementation.
- Production deployment was not performed.
- Implemented migration `0013_core_cache_receipts.sql` with pending one-time receipts and committed Core-owned cache entries.
- Added `cacheReceiptId` issuance for allowed full-price new/refresh decisions; exact `requestId + ticker` replay returns the stored receipt without a second charge.
- Added HMAC-only `POST /api/internal/access/cache/commit` with immutable-field validation, 15-minute receipt lifetime, same-digest idempotency, different-digest rejection, and server-owned cache timestamps.
- Access pricing now ignores scanner cache hints and grants cached pricing only from an unexpired matching `core_cache_entries` row.
- Added dev-only repeatable fixture `scripts/dev/seed-test-user.sql`; applied it to dev D1 with non-deliverable `.invalid` email, no Telegram identity, API key, token, or production personal data. Re-run resets fixture-owned sessions, API keys, quota decisions, receipts, and cache entries.
- Verified migration `0013` locally and in dev D1; verified the fixture has an active `pro` subscription through 2026-07-31. No production migration or Worker deployment was performed.
- Read-only dev verification confirmed `telegram_user_id = null`, `session_count = 0`, and `api_key_count = 0` for the fixture.
- Verified `npm run check` and all 45 tests pass after receipt/commit implementation.
- Added mandatory endpoint-level internal scopes through `INTERNAL_API_SCOPES_JSON`; scope denial returns `403 internal_scope_required` before nonce consumption. Cross-service negative tests cover matcher-to-scanner and scanner-to-matcher attempts.
- Implemented HMAC-only `POST /api/internal/deliver` with strict payload keys, Core-resolved recipients, live user access recheck, D1 idempotency ledger, audit, duplicate suppression, safe explicit retry, and blocked retry for indeterminate Telegram outcomes.
- Added admin-managed `route_id` and `telegram_chat_id` for channel delivery without accepting raw destinations from matcher payloads.
- Implemented Cloudflare Email Service verification with a 15-minute one-time HMAC-hashed token, verified-email session issuance, and account-link conflict protection.
- Added free `beta` policy with 25 user units / 50 half-credits; verified email users without active access receive a non-expiring beta subscription.
- Added strict future ISO validation for API-key `expiresAt` and removed the variable-length early exit from the Web Crypto comparison helper.
- Added migration `0014_internal_delivery_email_beta.sql`; all 12 commands applied successfully to local D1.
- Verified `npm run check` and all 55 tests pass after scopes, delivery, email verification, API expiry, and beta implementation.

Not completed:

- Local Wrangler production dry-run could not read the sandboxed Windows workspace path; CI bundle validation remains required before any approved release.
- Роман завершил проверку и заблокировал выпуск до устранения P0/P1.
- Остальные исправления P0/P1 и повторная проверка Романа ещё не выполнены.
- Интеграционная проверка `stock-signal-scanner` ещё не завершена.
- Реальный Workers-runtime/D1 тест конкурентного commit ещё не выполнен; unit tests cover idempotent same-digest replay, mismatch, used receipt, expiry, and forged scanner cache hints.
- Повторная проверка receipt/commit Романом и Леной ещё не выполнена.

Blockers:

- Migration `0008_scanner_access_quota.sql` must be applied before deploying the Worker code.
- Остальные P0: отсутствует безопасный Google OAuth, усиленная защита админки и защита платёжных уведомлений от повторов.
- Cloudflare Email Service выбран и код binding готов; до live dev-проверки требуется onboard sender domain и задать `EMAIL_FROM`/`EMAIL_VERIFICATION_SECRET` без публикации значений.
- `telegram_company_matcher_app` and `stock-signal-scanner` must deploy the new HMAC canonical format and unique request IDs together with this Worker; old Bearer calls now fail closed.
- Production remains blocked until Roman reviews the implemented Core receipt/ledger model and Lena completes the live dev integration flow with `stock-signal-scanner`.

Risks:

- `/api/health` depends on the SaaS core migration being applied because it now checks `api_keys` and `analysis_requests`.
- Email endpoint fail-closed отвечает `503`, пока Cloudflare Email Service sender domain и секреты окружения не настроены.
- The seeded MVP quota numbers are explicit implementation defaults and must be confirmed by the product manager before paid launch.
- Unknown or inactive subscription plans fail closed with `internal_access_unavailable` until a policy is configured in `quota_plan_policies`.

Needs from manager:

- Confirm first supported country/language launch list remains Israel Hebrew and US Russian.
- Confirm payment provider direction so subscription lifecycle statuses can be mapped precisely.
- Confirm whether website SaaS login will use Telegram-only auth, email magic link, password auth, or an external identity provider.
- Confirm or replace the seeded MVP quota limits and report costs before paid launch.
- Ask Roman to review POST body HMAC, D1 quota atomicity, and fail-closed behavior.
- Ask the `stock-signal-scanner` manager to verify the exact request/response and HMAC canonical string.
- Select the transactional email provider and sender domain for confirmation links/codes.
- Provide Google OAuth client ID, client secret, and approved redirect URLs before OAuth implementation.
- Agree with Roman on administrator MFA method before admin session hardening.

Links/files:

- `src/index.ts`
- `src/index.test.ts`
- `README.md`
- `docs/integration-contract.md`
- `migrations/0006_saas_core.sql`
- `migrations/0008_scanner_access_quota.sql`
- `migrations/0009_verified_email_identity.sql`
- `migrations/0010_pending_email_identity.sql`
- `migrations/0011_api_auth_and_internal_replay.sql`
- `migrations/0012_access_contract_v11_half_credits.sql`
- `migrations/0013_core_cache_receipts.sql`
- `migrations/0014_internal_delivery_email_beta.sql`
- `scripts/dev/seed-test-user.sql`

## Glasha - Designer

### Tasks

- [x] Prepare SaaS dashboard UX map.
- [x] Prepare first wireframe list for landing, pricing, signup, dashboard, API access, ticker analysis, reports, billing, admin.
- [x] Add legal/disclaimer UX points from Oleg's documents.
- [x] Define empty/loading/error/blocked/expired states.
- [x] Identify what must be Telegram WebApp versus full website.

### Report

Status: `Ready for product review`

Completed:

- Created `docs/saas-ux-v0.1.md`.
- Prepared SaaS dashboard UX map with dashboard modules and navigation relationships.
- Prepared first wireframe list for landing, pricing, signup/login, onboarding, dashboard, countries/channels, API access, ticker analysis, reports, billing, and admin monitoring.
- Added required legal/disclaimer placement points using Oleg's disclaimer UI points, production-ready legal microcopy, and forbidden phrase list.
- Defined empty, loading, error, blocked, expired, and rate-limited UX states.
- Defined what belongs in Telegram WebApp versus the full website.
- Added the UX document to `docs/documentation-index.md`.

Not completed:

- Pixel-perfect visual designs are not created yet.
- Final copy cannot be fully locked until manager confirms auth method, payment provider, and first supported countries/languages; MVP API commercial-use wording is already ready.

Blockers:

- Payment provider is not selected.
- Website auth method is not confirmed.
- First supported country/language launch list still needs manager confirmation.

Risks:

- Current implemented SaaS screens are useful as product skeletons, but legal notices and first-use acknowledgment UI still need implementation by Yasha before production.
- Telegram WebApp should stay lightweight; adding billing/API/report-heavy flows there would make the product harder to use.
- Any copy using hard-banned investment phrases must be blocked before launch.

Needs from manager:

- Confirm website auth model: Telegram-only, email magic link, password auth, or external identity provider.
- Confirm payment provider and checkout UX direction.
- Confirm first supported country/language launch list.
- API commercial usage policy is confirmed: display with attribution and restrictions is allowed; resale and redistribution require written agreement.

Links/files:

- `docs/saas-ux-v0.1.md`
- `docs/legal/disclaimer-ui-points.md`
- `docs/legal/ui-legal-microcopy.md`
- `docs/legal/forbidden-product-phrases.md`
- `docs/documentation-index.md`

## Lena - QA Engineer

### Tasks

- [x] Prepare v0.1 regression checklist.
- [x] Cover account registration and Telegram init data.
- [x] Cover subscription active/expired access.
- [x] Cover internal access auth failure.
- [x] Cover CORS allowlist behavior.
- [x] Cover rate limit behavior.
- [x] Add legal/disclaimer acceptance criteria from Oleg.
- [x] Подготовить и повторно проверить после `0013` матрицу контракта `1.1`: regular/FundRep, new/cached/refresh/own-repeat, no-quota и no-access.
- [x] Проверить HTTP-политику: бизнес-решения `200`, неверный payload `400`, неверная HMAC-авторизация `401/403`, внутренняя ошибка `5xx`, отсутствие `404` после deploy.
- [x] Проверить после `0013` cache TTL на границах 59/60/61 минута через Core-owned ledger и отсутствие повторного списания для `requestId + ticker`.
- [ ] Выполнить dev-регрессию цепочки scanner -> core access check -> analysis и matcher -> core после синхронного обновления HMAC.
- [ ] Подтвердить, что секреты отсутствуют в логах, ошибках, отчётах и тестовых снимках.
- [x] Проверить core-owned cache flow: new -> receipt -> successful commit -> cached discount; invalid/used receipt; commit mismatch; concurrent commit; expired entry; fake scanner hit без ledger entry.
- [ ] После реализации `/api/internal/deliver` проверить sent/duplicate/access_denied, changed payload, transport replay, Telegram failure и retry с тем же `deliveryId`/новым `X-Request-Id`.

Финальный интеграционный QA по заданию менеджера `stock-signal-scanner`:

- [ ] Проверить deployed dev Core и deployed dev Scanner вместе с отдельным scanner key ID; mocked/unit-only результат не считается сквозным PASS.
- [ ] Подтвердить, что scanner cache hints подписаны, но не влияют на скидку; скидка появляется только после Core receipt -> successful commit -> Core cache ledger.
- [ ] Проверить allow, no-access, no-quota, cache, refresh, own-repeat, regular и FundRep, включая фактическое изменение dev quota balance.
- [ ] Проверить недоступность Core, timeout, `401/403/409/5xx` и некорректный ответ: Scanner не выполняет анализ и не помечает запрос успешным.
- [ ] Выполнить production-like regression в изолированном dev: те же версии миграций, HMAC/scopes, retry/idempotency, health/log checks, без production secrets и записей.
- [x] Выдать release verdict: `PASS`, `PASS WITH RISKS` или `FAIL`; перечислить блокеры production и приложить безопасные evidence без секретов.

### Report

Status: `Ready with risks`

Completed:

- Проверена матрица regular/FundRep для new/cached/refresh/own-repeat, квоты, HTTP-статусы, TTL 59/60/61 и идемпотентность `requestId + ticker`.
- Добавлено недостающее автоматическое покрытие; менеджер независимо подтвердил TypeScript и `42/42` теста.
- Живой dev health и unsigned fail-closed smoke прошли.
- Secret-canary не обнаружен в перехваченном Core error log; реальные секреты не читались.
- Prepared the v0.1 regression checklist in `docs/qa-plan.md`.
- Split the QA gate into automated checks, manual release checks, and legal/disclaimer acceptance criteria.
- Covered account registration and Telegram init data with automated tests:
  - valid signed Telegram WebApp init data verification;
  - strict-mode block for unsigned Telegram registration;
  - successful Telegram WebApp registration with signed init data.
- Confirmed existing coverage for subscription active access, expired subscription block, and missing subscription block.
- Added automated coverage for `/api/internal/access` auth failure when internal auth is missing.
- Confirmed CORS allowlist coverage for configured origin versus unknown origin.
- Added automated rate-limit coverage for exhausted registration bucket returning `429`.
- Added Oleg's legal/disclaimer acceptance criteria to the QA plan:
  - visible risk disclaimer links on key surfaces;
  - required checkout/subscription risk acknowledgment;
  - non-prechecked legal checkboxes;
  - stored document version and timestamp;
  - first ticker analysis disclaimer acceptance;
  - Telegram, report, and API disclaimer requirements;
  - forbidden wording checks for buy/sell/hold, guaranteed profit, risk-free, and personal recommendation language.
- Verified `npm.cmd run check` passes.
- Verified `npm.cmd test` passes with 16 tests.

Not completed:

- Full manual staging regression is not executed because staging/prod Cloudflare resources are not provisioned yet.
- Payment-provider checkout, renewal, cancellation, refund, and invoice flows cannot be QA-approved until the provider and final flow are selected.
- Real Telegram webhook delivery, duplicate webhook replay, and Telegram API error handling still need staging verification with real bot secrets.
- Legal/disclaimer criteria are documented for QA, but product implementation of all disclaimer surfaces and persisted acceptances still needs design/dev follow-up.

Blockers:

- Dev D1 содержит тестового пользователя и активную подписку, но Worker с кодом `0013` ещё не развёрнут, поэтому live allowed/no-quota/receipt/commit пока не проверены.
- Deployed scanner/matcher ещё не передали подтверждённый HMAC integration report.
- Core-owned receipt/commit flow реализован и покрыт unit tests, но ещё не прошёл live dev QA и повторную проверку Романа.
- Payment provider is not selected.
- Staging environment, production-like secrets, Telegram bots, and D1 resources are not fully available for end-to-end QA.
- Business jurisdiction and final legal review are not confirmed.

Risks:

- Release is not `ready for production` until manual staging regression covers payment/access, Telegram webhook delivery, CORS production origins, rate limits, and admin monitoring.
- `ALLOW_UNVERIFIED_TELEGRAM` must be disabled in production; otherwise Telegram account binding can be abused.
- Internal endpoints must keep `INTERNAL_API_SECRET` configured and rotated per environment; missing secret returns 503 and blocks service integrations.
- Footer-only disclaimers are not enough for checkout, ticker analysis, Telegram reports, API outputs, or downloadable reports.
- Account endpoints currently rely on userId/email/Telegram lookup patterns; public website login/session hardening remains a production risk.

Needs from manager:

- Confirm payment provider and final subscription lifecycle states.
- Confirm first launch countries/languages for release test data.
- Confirm staging URL, allowed production origins, and Telegram bot secrets for QA run.
- Confirm whether v0.1 production gate requires real payment sandbox testing or simulated subscription events are enough.
- Confirm owner/date for implementing persisted legal acceptance fields and disclaimer UI surfaces.

Links/files:

- `docs/qa-plan.md`
- `src/index.test.ts`
- `docs/legal/disclaimer-ui-points.md`
- `docs/legal/ui-legal-microcopy.md`
- `docs/integration-contract.md`

### QA report: Core cache receipts and final scanner gate, 2026-07-01

Release verdict: `FAIL` для финальной интеграции `stock-signal-scanner -> Core`; локальный Core contract `1.1 + 0013` — `PASS WITH RISKS`.

Выполнено:

- `npm.cmd run check` прошёл; `npm.cmd test` — `45/45` tests passed.
- Локальная D1 поднята с миграциями `0001-0013` и dev-only фикстурой: пользователь `dev_test_scanner_user`, активная `pro` subscription, 1000 units, без Telegram identity, API key и session.
- Production-like Core прогон выполнен через реальный локальный Wrangler Worker, подписанные HMAC HTTP-запросы и реальную D1, без моков бизнес-flow.
- Fake scanner cache hit без Core ledger отклонён как источник скидки: ответ `new_regular`, `chargeUnits: 1`, выдан Core receipt.
- Проверен flow `new -> receipt -> commit -> cached`: `new_regular = 1`, два одновременных commit одного receipt с одинаковым digest вернули `200/200`, создана одна cache entry; следующий новый request получил `cached_regular = 0.5` и `cacheReceiptId = null`.
- Проверены receipt errors: invalid receipt = `400`, immutable mismatch = `400`, повтор с другим digest = `409`, expired pending receipt = `410` без cache entry.
- Проверен refresh: при существующем Core cache ответ `refresh_regular = 1` и выдан новый receipt.
- Проверен FundRep на реальной D1: `new_fundrep = 3`, business repeat = `own_repeat_fundrep`, `chargeUnits: 0`.
- Проверены `rejected_no_access` и `rejected_no_quota`; при исчерпанном остатке списание не выполнено, `remainingUnits: 0`.
- На контрольной точке ledger содержал ровно `7` credits после regular fake-hit/new/cached/refresh и одну committed cache entry, что совпало с ожидаемой суммой `2 + 2 + 1 + 2` credits.
- TTL после `0013`: 59-я минута — `cached_regular`; ровно 60 минут — cache hit в детерминированном тесте с frozen time и включительной границей; 61-я минута — `new_regular`. Scanner hint на 61-й минуте не изменил решение Core.
- Репозиторий, исходники, docs/reports и тестовые артефакты просканированы на private-key markers, Telegram token shape, API key shape и QA secret-canary: raw secrets не найдены. Синтетический локальный HMAC marker не сохранён в файлах. Реальные секреты не читались и не выводились.

Не выполнено:

- Deployed dev Core не содержит текущую Worker-версию `0013`: live `/api/health` отвечает `200`, D1 migration применена и fixture присутствует, но health response не содержит checks `cache_receipts` и `core_cache_entries`. Live receipt/commit QA заблокирован version drift.
- Deployed `stock-signal-scanner` ещё не передал подтверждённый HMAC integration report и, согласно его сектору, не подключил `POST /api/internal/access/check` и `POST /api/internal/access/cache/commit`.
- Не проверены scanner fail-closed реакции на Core timeout, malformed response, `401/403/409/5xx` и недоступность Core. Unit/Core-only результат не считается scanner E2E PASS.
- Не выполнена цепочка matcher -> Core -> Scanner -> cache commit -> delivery; `/api/internal/deliver` и delivery ledger ещё не реализованы.
- Проверка отсутствия секретов в deployed scanner/matcher logs и Telegram error path не выполнена, поэтому общий checkbox log safety остаётся открытым.

Блокеры production:

- Развернуть в dev текущий Core Worker с кодом `0013` и проверить, что health включает `cache_receipts/core_cache_entries`.
- Реализовать и проверить endpoint-level service scopes: scanner key только для access/cache, matcher key только для matcher/delivery routes.
- Обновить deployed Scanner на HMAC-only access/commit client, затем повторить матрицу с фактическим dev quota balance и scanner analysis result.
- Реализовать `/api/internal/deliver`, delivery idempotency и live test bot/route regression.
- Повторить безопасный log scan одновременно для Core, Scanner и Matcher.

QA решение:

- Core `0013` local runtime: `PASS WITH RISKS`.
- Deployed dev multi-service integration: `FAIL`.
- Production: `BLOCKED`.

## Max - DevOps / Cloudflare Engineer

### Tasks

- [x] Confirm dev/staging/production environment plan.
- [x] Confirm required secrets per environment.
- [x] Confirm D1 migration plan and backup/export approach.
- [x] Confirm Queues/DLQ requirements for matcher and delivery.
- [x] Confirm health check requirements.
- [x] Prepare first runbook list for deploy, rollback, webhook reset, DLQ replay, and secret rotation.
- [ ] Создать отдельный `API_KEY_HASH_SECRET` для dev и prod; секрет должен быть случайным, храниться только в Cloudflare Secrets и иметь план ротации.
- [ ] Создать `INTERNAL_API_SECRETS_JSON` с отдельными идентификаторами и секретами минимум для `telegram_company_matcher_app` и `stock-signal-scanner`; не использовать один общий ключ между сервисами.
- [x] Перед применением миграции `0011` подготовить порядок синхронного обновления core, matcher и scanner, проверку HMAC и план отката.
- [ ] Зафиксировать, что старые API-ключи требуют ротации после перехода на keyed hash; подготовить уведомление пользователям и операционную инструкцию.
- [x] Подтвердить версию Worker, которая сейчас развёрнута в production, и зафиксировать причину `404` на `POST /api/internal/access/check`; секреты в отчёт не включать.
- [x] Подготовить dev-развёртывание endpoint с миграциями `0008`-`0011`, HMAC-секретом scanner и smoke-тестами; production оставить заблокированным до решения по новому quota contract и одобрения Романа.
- [ ] Включить миграции `0012` и будущую `0013` в dev backup/migrate/rollback runbook; production не применять до PASS Романа и Лены.
- [x] После подготовки Яшей dev-only фикстуры применить её только к dev D1 и передать Лене идентификатор тестового пользователя/тариф без использования production-персональных данных.
- [ ] Устранить риск случайного production deploy: верхний уровень `wrangler.jsonc` и команда `npm run deploy` не должны по умолчанию указывать на production. Обычный локальный deploy должен требовать `--env dev`, а production оставаться только в защищённом ручном CI job.
- [ ] Увеличить или письменно обосновать трёхдневный срок хранения production D1 backup artifact; согласовать срок и доступ с Романом и требованиями privacy/accounting.

Проверка менеджера 2026-07-01:

- Живой dev `/api/health` независимо проверен: HTTP `200`, `environment=dev`, все D1 checks успешны.
- Неподписанный `POST /api/internal/access/check` независимо проверен: HTTP `401`, endpoint существует и работает fail-closed.
- Production не изменялся; `404` признан version drift старого Worker, а не отказом инфраструктуры.

### Report

Status: `Ready with infrastructure risks`

Completed:

- Confirmed the v0.1 environment plan: separate `dev`, `staging`, and `production` Workers, D1 databases, queues/DLQs, Telegram bots/chats, secrets, and domains/subdomains.
- Confirmed required secrets per environment, including `ADMIN_TOKEN`, `INTERNAL_API_SECRET`, `SUBSCRIPTION_WEBHOOK_SECRET`, `TELEGRAM_WEBHOOK_SECRET`, Telegram bot tokens, scanner shared secrets, and future payment provider secrets.
- Confirmed D1 migration flow: local/dev first, staging validation, production export before migration, manual approval before production, then `/api/health` verification.
- Confirmed Queue/DLQ requirements for matcher scan and delivery flows, with `requestId` as the idempotency key and manual DLQ replay for v0.1.
- Confirmed health check requirements: `/api/health` must return HTTP 200 with `ok: true`, environment, and D1 checks for core tables.
- Prepared first runbook list for deploy, rollback, Telegram webhook reset, DLQ replay, and secret rotation.

Not completed:

- Actual Cloudflare dev/staging resources are not yet created or wired into `wrangler.jsonc`.
- Production approval gate and full multi-environment CI/CD flow are not yet implemented.
- Payment provider secrets cannot be finalized until the payment provider is selected.

Blockers:

- Payment provider is not selected.
- Cloudflare resource IDs for dev/staging/prod need to be provisioned or confirmed before final production wiring.

Risks:

- Current `wrangler.jsonc` is production-named and points to one D1 database; it does not yet define separate `env.dev`, `env.staging`, and `env.production`.
- Current GitHub deploy workflow runs typecheck and deploy, but does not run `npm test` and does not require manual production approval.
- If production D1 migrations are run without export and approval, there is data-loss/paid-service outage risk.
- Production must not launch until CORS allowlist, production secrets, `ALLOW_UNVERIFIED_TELEGRAM=false`, health monitoring, DLQ monitoring, and rollback path are verified.

Needs from manager:

- Confirm Cloudflare account/project naming convention and target production domains/subdomains.
- Confirm payment provider direction.
- Confirm who approves production deploys.
- Confirm first supported countries/languages so Telegram bot secrets and bot routes can be finalized per environment.

Links/files:

- `docs/devops-cloudflare-plan.md`
- `docs/production-checklist.md`
- `wrangler.jsonc`
- `.github/workflows/deploy.yml`

Дополнение Макса, 2026-07-01:

- Применена штатная повторяемая фикстура `scripts/dev/seed-test-user.sql` командой `npm run db:seed:test-user:dev`, жёстко направленной на `market-signal-ai-bot-dev-db` с `--env dev --remote`.
- Перед применением сохранён dev backup `.wrangler/backups/market-signal-ai-bot-dev-db-before-fixture-2026-07-01.sql`: `15797` bytes, SHA-256 `36FDAE7177A54DCA7E0918C896E4CC27133B03D12503C6B07E7DA5D8D2020A67`.
- Обязательная для штатной фикстуры миграция `0013_core_cache_receipts.sql` применена только к dev D1; Worker не разворачивался.
- Фикстура применена только к dev D1 `4840d883-6136-40b6-8eb9-65ccede85670`.
- Данные для QA: user ID `dev_test_scanner_user`, email `scanner-fixture@example.invalid`, subscription ID `dev_test_scanner_subscription`, plan `pro`, status `active`, quota `2000` credits, used `0`.
- Фикстура не содержит production-персональных данных, API keys или secrets; при повторном запуске очищает только quota/cache state собственного fixture user.
- Read-only проверка production D1 `ebc41219-4276-4068-adb4-1a78d0f0192f` вернула `fixture_rows=0`; production schema и данные не изменялись.

Дополнение Макса, 2026-07-02:

- Актуальный Core развёрнут в `market-signal-ai-bot-dev`; активная версия после синхронной secret rotation: `43d35e9e-7687-47e3-aa27-6ea7884ebdcb`, 100% traffic.
- Перед deploy прошли `npm run check`, `49/49` тестов и Wrangler strict dry-run.
- К dev D1 применена миграция `0014_internal_delivery_email_beta.sql`; pending migrations отсутствуют, `/api/health` возвращает `200`, `ok=true`, `environment=dev`.
- Перед `0014` создан dev export `.wrangler/backups/market-signal-ai-bot-dev-db-before-0014-2026-07-02.sql`: `18902` bytes, SHA-256 `B0107745B35C83E589078EC329F41A0394F6C66CC3F82D2A107807DB92705288`.
- Создан отдельный случайный `API_KEY_HASH_SECRET` для Core dev. Production secret не менялся.
- Scanner dev переведён на отдельную синхронизированную пару `scanner-dev-v2` / HMAC secret через Cloudflare Secrets; raw values не выводились и временные payload-файлы удалены.
- Положительный HMAC smoke `POST /api/internal/access/check` прошёл: HTTP `200`, `allowed=true`, `quotaDecision=new_regular`, Core выдал cache receipt. После smoke dev fixture quota/cache state сброшен повторным seed.
- Неподписанные access и cache-commit запросы возвращают `401`; CORS preflight для `https://beta.market-signal.ai` возвращает разрешённый origin.
- Создание custom domain `beta.market-signal.ai` заблокировано: родительский домен `market-signal.ai` возвращает DNS `NXDOMAIN`, активной Cloudflare zone нет. Cloudflare API отклонил domain trigger; нерабочий route удалён из `wrangler.jsonc`, чтобы последующие dev deploy не падали.
- Рабочий beta fallback до регистрации и подключения зоны: `https://market-signal-ai-bot-dev.fnemoy.workers.dev`.
- Production Worker/D1/secrets не изменялись; production остаётся на версии `967192f3-12cc-4176-8dc5-ceb0b0d21ae7` и заблокирован.

DevOps статус: `Готово с инфраструктурным риском`. Dev работает; beta custom domain требует регистрации `market-signal.ai` и добавления зоны в текущий Cloudflare account.

Проверка менеджера после отчёта Макса:

- 2026-07-02 независимо подтверждён dev health: HTTP `200`, `ok=true`, `environment=dev`.
- Health checks подтверждают доступность таблиц `cache_receipts`, `core_cache_entries`, `internal_deliveries`, `email_verifications` и остальных обязательных D1-таблиц после `0013/0014`.
- Неподписанный `POST /api/internal/access/check` независимо возвращает `401 internal_key_and_request_id_required`; endpoint работает fail-closed.
- Текущие локальные `npm run check` и `49/49` тестов проходят.
- Развёртывание актуального Core в dev и базовая настройка Scanner HMAC/API key hash больше не являются блокерами.
- Custom domain не блокирует закрытую beta: до регистрации и подключения `market-signal.ai` используется `https://market-signal-ai-bot-dev.fnemoy.workers.dev`.
- Live receipt/commit через фактически развёрнутый Scanner, endpoint scopes и повторный verdict Романа/Лены остаются release gates.

Проверка менеджера по `CORE-P0-ACCESS-CHECK-DEV-404`, 2026-07-08:

- Core dev URL подтверждён: `https://market-signal-ai-bot-dev.fnemoy.workers.dev`.
- Current Core dev deployment: `a04d7111-f9fa-4586-97a0-50545e5bc225`, 100% traffic, source `Secret Change`, created `2026-07-08T09:14:36Z`.
- В deployed Core version присутствует handler `scannerAccessCheck`; значит route `POST /api/internal/access/check` включён в текущий dev bundle.
- Safe bindings подтверждены без раскрытия секретов: `ENVIRONMENT=dev`, `INTERNAL_API_KEY_ID=scanner-dev-v2`, dev D1 `4840d883-6136-40b6-8eb9-65ccede85670`, secrets `INTERNAL_API_SECRETS_JSON`, `INTERNAL_API_SCOPES_JSON`, `INTERNAL_API_SECRET`, `API_KEY_HASH_SECRET` существуют.
- Health check: `GET /api/health` вернул `200`, `ok=true`, `environment=dev`.
- Неподписанный `POST /api/internal/access/check` вернул `401 {"error":"internal_key_and_request_id_required"}`; это ожидаемо и доказывает, что endpoint live и не возвращает `404`.
- Запрос с `scanner-dev-v2`, но неверной подписью вернул `401 {"error":"unauthorized"}`; это ожидаемо и доказывает, что invalid HMAC не даёт `404`.
- Локальная проверка Core после этого прошла: TypeScript `PASS`, тесты `55/55 PASS`.
- Активный deployed scanner dev version `96bcb54a-d742-437e-9ad0-3c20d288775d` содержит safe vars: `ACCESS_CHECK_URL=https://market-signal-ai-bot-dev.fnemoy.workers.dev/api/internal/access/check`, `CORE_HMAC_KEY_ID=scanner-dev-v2`, `APP_ENV=dev`; `CORE_HMAC_SECRET` существует как secret binding.
- Вывод: текущий `404` больше не подтверждается на стороне Core dev. Если Scanner всё ещё получает `404`, нужно проверить фактический runtime-запрос scanner: полный URL до отправки, method `POST`, отсутствие лишнего base path, route через production Core, старый preview URL или старую Worker version.

Проверка менеджера по `CORE-P0-SIGNED-SCANNER-REQUEST-404`, 2026-07-08:

- Scanner сообщил requestId `devops-core-live-regular-a3e04e85-fb5c-45a7-ac6f-1d7d43bf1f1c`, scanner dev version `105cd2c1-3d35-4aa2-9036-38c10f43cf9a`, Core version `a04d7111-f9fa-4586-97a0-50545e5bc225`, checked at `2026-07-08T15:07:27.7994879+04:00`.
- По текущему Core dev deployment status активная версия уже `73345693-a27c-4705-1b703ac03d10`, 100% traffic, deployment created `2026-07-08T11:07:27Z`, то есть почти в тот же момент, что scanner check. Версия `73345693` тоже содержит handler `scannerAccessCheck`.
- Текущий live Core dev health: `GET /api/health` -> `200`, `ok=true`, `environment=dev`.
- Текущий live Core dev exact route: `POST /api/internal/access/check` без HMAC -> `401 {"error":"internal_key_and_request_id_required"}`; route существует.
- Path sensitivity check: `POST /api/internal/access/check/` с trailing slash -> `404 {"error":"not_found"}`; `GET /api/internal/access/check` -> `404 {"error":"not_found"}`; `POST /api/internal/access/check?x=1` -> `401`. Значит query не ломает route, но trailing slash или wrong method дают ровно такой `404` до HMAC.
- D1 trace по business requestId в `quota_decisions_v11`: не найден. Это значит, что запрос не дошёл до `scannerAccessCheck` business logic.
- D1 trace по `internal_request_nonces` с этим requestId: не найден. Последняя найденная nonce-запись для `scanner-dev-v2` относится к старому smoke, не к этому requestId. Это значит, что валидная HMAC-проверка для указанного requestId в Core не завершалась.
- Core historical request log по прошедшему времени недоступен через Wrangler tail; для точного подтверждения нужен повтор scanner request во время активного live tail или временный dev-only sanitized route logging.
- Предварительный вывод: причина `404` с высокой вероятностью не HMAC/secret map, а фактический runtime path/method/deployment race. Самый похожий воспроизводимый случай на Core: trailing slash `/api/internal/access/check/` или wrong method. Если scanner реально отправляет exact `POST /api/internal/access/check`, нужен повтор под live tail.
- Безопасные данные для Scanner team: Core dev URL `https://market-signal-ai-bot-dev.fnemoy.workers.dev`; active key id `scanner-dev-v2`; expected canonical path `/api/internal/access/check`; secrets/signatures/tokens не раскрывались.

Решение Core по просьбе Scanner, 2026-07-08:

- Добавлен alias route: `POST /api/internal/access/check/` теперь обрабатывается тем же handler, что и `POST /api/internal/access/check`.
- Контракт не меняется: основной expected path остаётся `/api/internal/access/check`.
- HMAC строго привязан к фактическому path запроса. Если scanner отправляет `/api/internal/access/check/`, canonical string должен содержать `/api/internal/access/check/`. Подпись на path без trailing slash для запроса с trailing slash отклоняется.
- Добавлены regression tests: trailing slash с корректно подписанным path доходит до scanner access handler; trailing slash с подписью на path без slash получает `401 unauthorized`.
- Проверки Core: TypeScript `PASS`, тесты `58/58 PASS`.
- Dev deploy ещё не выполнен в рамках этой записи; перед повторным signed E2E Scanner нужен deploy Core dev и live tail.

## Марина - консультант по юрисдикции бизнеса и платежам

### Задачи

- [x] Подготовить сравнение платежных провайдеров для SaaS-подписок.
- [x] Подготовить матрицу выбора бизнес-юрисдикции.
- [x] Проверить, подходят ли Stripe, Paddle, Lemon Squeezy, PayPal/Braintree, Recurly и Telegram Payments под нашу модель.
- [x] Уточнить доступность провайдеров для возможных юрисдикций бизнеса.
- [x] Выписать вопросы по счетам, возвратам, отмене подписки, налогам, НДС и sales tax.
- [x] Подготовить рекомендацию по платежному провайдеру для MVP.
- [ ] Запустить Paddle approval как первый Merchant of Record provider.
- [ ] Подготовить Paddle onboarding/KYC checklist.
- [ ] Подготовить Delaware C-Corp setup checklist вместе с Натальей.

### Отчёт

Статус: `Готово с внешними legal/tax блокерами`

Сделано:

- Подготовлен отчёт для менеджера: `docs/legal/reports/task-08-business-jurisdiction-payment-report.md`.
- Подготовлены вопросы для локального юриста и бухгалтера: `docs/legal/reports/task-09-local-counsel-accountant-questions.md`.
- Рекомендован практический launch-route: US / Delaware company + Paddle или Lemon Squeezy как Merchant of Record для первого платного запуска.
- Stripe direct рекомендован как более поздний вариант после готовности tax, invoice, sales tax и VAT операций.
- Зафиксировано, что продукт должен позиционироваться как informational SaaS analytics, а не как personalized investment advice.
- Подтверждено, что Georgia может быть приемлема для low-cost MVP, но слабее для Stripe-led launch.
- Israel не рекомендован без реальной связи с Израилем: основатели, команда, инвесторы, клиенты или операционное присутствие.

Не сделано:

- Нет финального заключения лицензированного локального юриста.
- Нет финального заключения бухгалтера/налогового консультанта.
- Payment provider direction confirmed: Paddle approval first; Lemon Squeezy remains backup.
- Не подтверждены KYC-документы и банковский путь для выбранной юрисдикции.

Блокировки:

- Нельзя финально утверждать платный запуск, пока Paddle не даст MoR approval.
- Нужно подготовить KYC-документы и банковский путь для Delaware C-Corp.
- Нужно подтвердить founder/local tax residency review и CPA setup.

Риски:

- Direct Stripe с первого дня может создать лишнюю нагрузку по VAT, sales tax, invoice/receipt и refund operations.
- Paid securities-related analytics остаётся рядом с investment adviser perimeter, особенно при US users.
- Если продукт начнёт давать personalized advice, portfolio-specific recommendations, auto-trading или execution, legal risk profile резко изменится.

Нужно от менеджера:

- Подтвердить, кто фактически занимается Delaware C-Corp formation provider / registered agent.
- Подтвердить, кто будет подавать Paddle application.
- Подтвердить банковский путь для Delaware C-Corp.

Ссылки/файлы:

- `docs/legal/reports/task-08-business-jurisdiction-payment-report.md`
- `docs/legal/reports/task-09-local-counsel-accountant-questions.md`

## Наталья - локальный юрист и налоговый бухгалтер

### Задачи

- [x] Ответить на вопросы в `docs/legal/reports/task-09-local-counsel-accountant-questions.md` прямо под каждым пунктом в поле `Answer:`.
- [x] Подтвердить рекомендуемую структуру компании для MVP: Delaware, UK, Estonia/Ireland, Georgia или другой вариант.
- [x] Проверить налоговую резидентность, permanent establishment risk, payroll/branch/local registration risk.
- [x] Подтвердить обязательные filings, bookkeeping records, annual reports, tax returns и beneficial ownership filings.
- [x] Проверить, может ли выбранная компания подключить Stripe, Paddle и Lemon Squeezy, и какие документы нужны для KYC.
- [x] Подтвердить, как учитывать Merchant of Record payouts, комиссии, refunds, chargebacks и revenue recognition.
- [x] Проверить VAT, sales tax, GST, local tax и различия между B2B/B2C продажами.
- [x] Подтвердить требования к invoice/receipt, cancellation flow, refund policy, trials и automatic renewal wording.
- [x] Проверить, можно ли запускать продукт как informational SaaS analytics без investment adviser / financial adviser / broker-dealer регистрации.
- [x] Назвать страны или типы клиентов, которые нужно временно заблокировать до дополнительного legal review.
- [x] Дать финальный статус: `Юрисдикция и налоги готовы`, `Готово с рисками` или `Заблокировано`.
- [ ] Подготовить Delaware C-Corp setup checklist.
- [ ] Подготовить CPA/bookkeeper setup checklist для MoR accounting.
- [ ] Подготовить founder/local tax residency review checklist.

### Отчёт

Статус: `Готово с рисками`

Сделано:

- Заполнены все поля `Answer:` в `docs/legal/reports/task-09-local-counsel-accountant-questions.md`.
- Подтверждена ключевая рекомендация: Delaware C-Corp + Paddle или Lemon Squeezy как Merchant of Record + informational SaaS analytics.
- Рекомендовано не использовать direct Stripe на первом платном запуске.
- Подтверждено, что персонализированные инвестиционные функции, auto-trading, copy trading, portfolio-specific recommendations и broker/order-routing workflows нельзя включать в MVP без отдельного legal review.
- Проверены KYC, MoR payouts, VAT/sales tax/GST, invoice/receipt, refund/cancellation, trial/renewal wording и B2B/B2C различия на уровне practical launch review.

Не сделано:

- Нет подписанного внешнего legal opinion или CPA tax memo; текущий review является practical launch review.
- Не получено финальное MoR approval от Paddle.
- Не завершён founder/local tax residency review.
- Не завершён securities/investment-adviser perimeter review для публичного paid launch.
- Не настроен CPA-led monthly MoR accounting setup.

Блокировки:

- Нельзя запускать paid subscriptions до MoR approval, founder/local tax residency review, securities perimeter review и бухгалтерского setup для MoR payouts/refunds/deferred revenue.

Риски:

- Delaware C-Corp подходит как рекомендуемая структура, но может возникнуть дополнительная налоговая резидентность или permanent establishment в стране, где фактически работают founders/management.
- Direct Stripe переносит VAT/sales tax/GST/invoice burden на компанию, поэтому не рекомендован для первого платного запуска.
- Paid securities-related analytics остаётся чувствительной зоной; продукт должен оставаться impersonal/informational.
- API customers могут создать downstream regulated-use risk, если используют outputs для personalized advice, paid signal resale, broker execution или advisory services.

Нужно от менеджера:

- Подтвердить конкретного formation provider / registered agent для Delaware C-Corp.
- Подтвердить конкретного CPA/bookkeeper.
- Подтвердить страны и людей, которые входят в founder/local tax residency review.
- Подтвердить, что MVP не включает персонализированные investment features, portfolio import with recommendations, auto-trading, copy trading или broker integrations.

Ссылки/файлы:

- `docs/legal/reports/task-09-local-counsel-accountant-questions.md`
- `docs/legal/reports/task-08-business-jurisdiction-payment-report.md`

## Сергей - бухгалтер по SaaS и Merchant of Record учёту

### Задачи

- [x] Подготовить CPA/bookkeeper setup checklist для Paddle как Merchant of Record.
- [x] Подготовить chart of accounts для gross customer sales, net payouts, Paddle fees, refunds, chargebacks, chargeback fees, taxes collected/remitted by Paddle, deferred revenue и bank reconciliation.
- [x] Описать monthly close process: какие Paddle reports скачивать, где хранить, как сверять с банком и подписками.
- [x] Подготовить список документов, которые нужны внешнему CPA/bookkeeper.
- [x] Согласовать с Натальей, какие tax/accounting пункты обязательны до первой оплаты.
- [x] Согласовать с Мариной, какие Paddle payout/tax/refund reports нужны для учёта.
- [x] Дать статус: `Accounting setup ready`, `Готово с рисками` или `Заблокировано`.

### Отчёт

Статус: `Готово с рисками`

Сделано:

- Подготовлен рабочий accounting setup для Paddle Merchant of Record: `docs/accounting-mor-setup.md`.
- Подготовлен CPA/bookkeeper setup checklist для Delaware C-Corp SaaS + Paddle MoR.
- Подготовлен chart of accounts для cash, Paddle clearing, MoR receivables, deferred revenue, SaaS/API revenue, gross customer sales management reporting, refunds, chargebacks, Paddle fees, chargeback fees, MoR-collected taxes, FX и bank fees.
- Описан monthly close process: выгрузка Paddle reports, хранение в monthly close папке, сверка gross sales/refunds/fees/taxes/net payout, сверка payout deposits с банком, subscription/deferred revenue review и exception review.
- Подготовлен список документов для внешнего CPA/bookkeeper: company/tax records, Paddle contract/KYC/reports, product/pricing/subscription records, bank statements, reconciliation workbooks и policy versions.
- Согласованы с Натальей обязательные tax/accounting пункты до первой оплаты: CPA/bookkeeper selection, MoR payout treatment, deferred revenue, Paddle tax responsibility language, запрет direct sales outside Paddle до отдельного VAT/sales tax review, founder/local tax residency review.
- Согласованы с Мариной нужные Paddle reports: payout/settlement, transaction/sales, fee, refund/credit, chargeback/dispute, tax, subscription lifecycle, product/price catalog exports.
- Добавлен accounting setup документ в `docs/documentation-index.md`.

Не сделано:

- Не выбран внешний CPA/bookkeeper.
- CPA ещё не утвердил финальный chart of accounts, revenue recognition policy и monthly close workflow.
- Paddle approval ещё не получен для exact product.
- Реальные Paddle account exports ещё не проверены на доступность и формат.
- Founder/local tax residency review ещё не завершён вместе с Натальей.

Блокировки:

- Нельзя принимать первую оплату, пока не выбран CPA/bookkeeper, не подтверждён Paddle approval и не утверждён MoR accounting workflow.
- Нельзя использовать direct Stripe/direct sales без отдельного VAT/sales tax/invoice setup.

Риски:

- Если учитывать только net bank deposits без Paddle gross sales/refund/fee/tax reports, выручка, refunds, chargebacks и deferred revenue будут несверяемыми.
- Annual subscriptions требуют deferred revenue schedule; без него выручка может быть признана слишком рано.
- Paddle как MoR обычно снимает большую часть customer indirect tax burden, но company income tax, local tax residency, PE/payroll и corporate filings остаются на компании.
- API/B2B custom deals могут потребовать отдельного invoice/revenue recognition review.

Нужно от менеджера:

- Назначить внешнего CPA/bookkeeper.
- Подтвердить, кто получает и архивирует Paddle monthly reports.
- Подтвердить accounting system и банк для Delaware C-Corp.
- Подтвердить owner monthly close и deadline закрытия.
- Подтвердить, что до отдельного tax review все paid sales проходят только через Paddle MoR.

Ссылки/файлы:

- `docs/accounting-mor-setup.md`
- `docs/legal/reports/task-09-local-counsel-accountant-questions.md`
- `docs/legal/reports/task-08-business-jurisdiction-payment-report.md`

## Роман - инженер по кибербезопасности и безопасности приложения

### Задачи

- [x] Подготовить модель угроз для входа через Telegram.
- [x] Подготовить модель угроз для входа через Google.
- [x] Подготовить требования к безопасности сессий личного кабинета.
- [x] Проверить требование обязательного email для всех типов аккаунтов.
- [x] Проверить жизненный цикл API-токена: создание, хеширование, показ только префикса, права доступа, ротация, отзыв и логи использования.
- [x] Проверить авторизацию внутренних эндпоинтов и защиту от повторного воспроизведения запросов.
- [x] Проверить требования безопасности для админки.
- [x] Подготовить дополнения к production-чеклисту по безопасности.
- [x] Проверить реализацию `POST /api/internal/access/check` после передачи от Яши: внутренний endpoint не должен быть доступен без сервисной авторизации.
- [x] Проверить, что подпись внутреннего POST-запроса включает digest тела, timestamp и nonce/request id, а устаревшие и повторно воспроизведённые запросы отклоняются.
- [x] Проверить, что `requestId` обеспечивает идемпотентность и параллельные одинаковые запросы не приводят к двойному списанию квоты.
- [x] Проверить, что endpoint не доверяет тарифу, балансу, ownership или quota-данным из публичного payload и получает их только из данных `market-signal-ai-bot`.
- [x] Проверить поведение fail-closed: при ошибке авторизации, базы данных, неизвестной версии контракта или невозможности подтвердить доступ возвращается запрет без списания лишних единиц.
- [x] Проверить отсутствие чувствительных данных и секретов в логах; разрешить журналировать только безопасные идентификаторы и итог решения.
- [x] Записать результат проверки в этот файл со статусом `Безопасность готова` либо `Заблокировано`, указав найденные проблемы, их критичность и ответственного за исправление.
- [x] После исправлений Яши повторить негативные тесты и выдать новый итоговый статус.
- [x] Проверить исправление P0 по email: отсутствие email-only сессии, отзыв старой неподтверждённой сессии, запрет поиска и привязки подписки по `pending_email`, а также сохранение проверенного Telegram-входа.
- [x] После проверки отметить этот P0 как `Закрыто` либо вернуть Яше конкретные замечания; общий security-gate не снимать, пока остальные P0/P1 открыты.
- [x] Проверить новый `POST /api/analysis/requests`: обязательный API-ключ, серверный scope `analysis:write`, отзыв, срок действия, пользователь, подписка, квота и лимиты по ключу/пользователю/IP.
- [x] Проверить создание и атомарную ротацию API-ключа, keyed hash через `API_KEY_HASH_SECRET`, одноразовый показ токена и отказ для старого ключа до ротации.
- [x] Проверить HMAC-only для внутренних endpoint-ов: отсутствие Bearer-обхода, обязательные `X-Key-Id`/`X-Request-Id`, привязку метода, пути, query и тела, а также защиту от повторного воспроизведения через миграцию `0011`.
- [x] Выдать отдельный результат по двум пунктам: `API token P0` и `internal HMAC P1` со статусом `Закрыто` либо перечнем замечаний Яше.
- [x] Проверить итоговый production access contract и подтвердить, что требование scanner о Bearer token заменено на HMAC-only без снижения защиты.
- [x] Проверить модель cache/repeat/refresh и убедиться, что scanner не может произвольно занизить списание, а повтор запроса не списывает квоту дважды.
- [x] После реализации `0013` проверить core-owned receipt/commit flow: scanner не может получить скидку без ранее выданного полного разрешения и committed cache entry.
- [x] Проверить одноразовость receipt, совпадение immutable fields, server-owned timestamps, concurrent commit и отсутствие скидки по одним cache hints.
- [x] Проверить разделение полномочий HMAC key ID между scanner и matcher; валидная подпись чужого сервиса не должна давать доступ к endpoint.

Финальный security review по заданию менеджера `stock-signal-scanner`:

- [x] Сверить canonical string Core и Scanner байт-в-байт: timestamp, key ID, transport request ID, method, pathname, canonical query и SHA-256 raw body.
- [x] Проверить HMAC-SHA256 на валидной подписи, неверном секрете, подмене каждого подписанного компонента и несовпадающем body digest.
- [x] Проверить clock skew: допустимое окно не шире +/-5 минут; просроченный и слишком будущий timestamp отклоняются.
- [x] Проверить replay protection и nonce storage: атомарная уникальность `keyId + X-Request-Id`, срок хранения не меньше окна подписи, безопасная очистка и конкурентный replay.
- [x] Проверить key rotation: overlap старого/нового key ID без общего секрета, отзыв старого ключа после smoke и разделение dev/production.
- [x] Проверить constant-time verification текущей реализации через Web Crypto и fixed-length byte comparison; исключить прямое сравнение подписи строковым оператором.
- [x] Повторно подтвердить отсутствие downgrade на Bearer и endpoint scopes между scanner/matcher.
- [x] Выдать один итог: `PASS`, `PASS WITH RISKS` или `FAIL`. Для каждого риска указать критичность, владельца, срок и влияние на production.

Передано на проверку 2026-06-29:

- Яша завершил реализацию `POST /api/internal/access/check`, миграцию `0008_scanner_access_quota.sql`, привязку HMAC к телу запроса, защиту от повторного списания и автоматические тесты.
- Менеджер проекта независимо подтвердил: TypeScript-проверка проходит, 24 автоматических теста проходят, локальная D1 сообщает, что все миграции уже применены.
- Роману необходимо проверить пункты выше и дать отдельное заключение по безопасности до подключения в рабочей среде.
- Яша передал отдельное исправление P0 по неподтверждённому email с миграциями `0009` и `0010`; менеджер проекта независимо подтвердил TypeScript, 27 тестов и локальные миграции.

### Отчёт

Статус: `Заблокировано - проверка безопасности 2026-06-29`

Примечание: ниже сохранён результат первичной проверки. Актуальный результат повторной проверки email-P0 приведён в подразделе `Повторная проверка P0 по email - 2026-06-29`.

Сделано:

- Завершена проверка приложения после работы Яши; TypeScript и все 24 теста проходят.
- Подтверждено, что закрытые API личного кабинета используют личность из серверной сессии, cookie защищена базовыми атрибутами, а изменяющие запросы требуют CSRF-токен.
- Подтверждены привязка HMAC к телу внутренних POST-запросов, базовая подпись платёжных уведомлений и безопасное хранение API-ключей.
- Обнаружен P0-захват аккаунта через неподтверждённый email и восемь дополнительных блокеров P0/P1.
- Подготовлен security-отчёт для менеджмента: `docs/security-manager-report-v1.0.md`.
- Проверены основные зоны риска: вход через Telegram, вход через Google, сессии сайта, API-токены, админка, внутренние эндпоинты, webhooks, scanner API, секреты и готовность к production.
- Подтверждены уже существующие положительные контроли: проверка Telegram init data, HMAC для subscription webhooks, internal auth, хеширование API-ключей, базовые rate limits, audit events и рекомендации по production-секретам.
- Зафиксирован главный security-блокер: аккаунтные API не готовы к коммерческому production, пока личность пользователя берётся из параметров запроса, а не из защищённой серверной сессии.

Не сделано:

- Реальная website session/authentication layer ещё не внедрена.
- Google OAuth ещё не реализован.
- CSRF-защита для будущих cookie-based сессий ещё не внедрена.
- Админ-аутентификация ещё требует усиления.
- Internal POST HMAC signatures ещё должны включать digest тела запроса.
- Subscription webhooks ещё требуют idempotency по provider event id.
- API-token enforcement, scopes, per-token limits и abuse detection ещё не завершены.

Блокировки:

- P0 на момент первичной проверки: `/api/register` выдавал сессию существующего аккаунта по переданному клиентом неподтверждённому email. Исправление повторно проверено и отмечено как `Закрыто` ниже.
- P0: отсутствуют безопасный Google OAuth, усиленная защита админки, защита платёжных уведомлений от повторов и применение API-ключей на endpoint анализа.
- P1: внутреннюю HMAC-защиту можно обойти общим Bearer-секретом; неполны защита от повторов, CSRF, жизненный цикл сессий и срок Telegram init data.
- Нельзя запускать публичный v1.0, пока не закрыты website sessions, CSRF, Google OAuth, admin hardening, webhook idempotency, internal body signatures и API-token enforcement.
- Главный инженерный приоритет: заменить `userId`, `email` и `telegramUserId` из запроса на identity из authenticated session во всех account/API-key маршрутах.

Риски:

- Пользователь потенциально может получить или изменить данные другого пользователя, если account endpoints доверяют переданным идентификаторам.
- Включение Google login без PKCE, `state`, `nonce`, проверки ID token и verified email создаст высокий риск захвата или неправильной привязки аккаунтов.
- Админ-токены в URL и слабая админ-аутентификация повышают риск компрометации админки.
- Без webhook idempotency повторные payment events могут некорректно менять paid access.

Нужно от менеджера:

- Подтвердить, что security-gate Романа является обязательным блокером v1.0.
- Назначить Яше задачу на реализацию защищённых website sessions и перевод account endpoints на session-derived identity.
- Назначить Яше задачу на Google OAuth только после согласования требований Романа.
- Назначить Максу задачу на production-секреты, rotation runbook, alerting и запрет секретов в логах.
- Решить, допускаем ли временный статус `Готово с рисками безопасности`; рекомендация Романа - не запускать публичный v1.0 до статуса `Безопасность готова`.

Ссылки/файлы:

- `docs/security-manager-report-v1.0.md`
- `docs/security-threat-model.md`
- `docs/production-checklist.md`

### Повторная проверка P0 по email - 2026-06-29

Статус P0 по email: `Закрыто`

Общий security-статус: `Заблокировано - остальные P0/P1 остаются открыты`

Проверено:

- Email-only запрос к `/api/register` возвращает `403 email_verification_required`, не ищет пользователя по email, не создаёт аккаунт или session и не устанавливает cookie.
- Миграция `0009_verified_email_identity.sql` добавляет признак подтверждения email и отзывает legacy sessions у пользователей без Telegram identity и без подтверждённого email.
- Миграция `0010_pending_email_identity.sql` переносит неподтверждённые адреса из `email` в `pending_email`; такой адрес больше не является login identity.
- `requireSession` дополнительно отзывает и отклоняет legacy email-only session, если миграционный отзыв по какой-либо причине не сработал.
- Поиск пользователя по email разрешён только при `email_verified_at IS NOT NULL`.
- Subscription webhook не создаёт и не привязывает подписку по неподтверждённому email или `pending_email`; без доверенного `userId`, Telegram identity либо verified email возвращается `409 verified_user_required`.
- Подписанный Telegram init data по-прежнему создаёт Telegram-authenticated session; переданный email сохраняется только как `pending_email` до подтверждения.

Негативные проверки:

- Email-only session issuance - blocked.
- Legacy unverified email-only session reuse - blocked и session отзывается.
- Subscription attachment по unverified email - blocked, запись подписки не создаётся.
- Telegram signed login regression - passed.

Результаты команд:

- `npm run check` - passed.
- `npm test` - passed, 27 tests.
- `wrangler d1 migrations list --local` - `No migrations to apply` для миграций `0009` и `0010`.

Заключение Романа:

- Конкретный P0 account takeover через совпадение неподтверждённого email устранён; замечаний для возврата Яше по этому исправлению нет.
- Общий security-gate не снимается. До public/paid v1.0 остаются обязательными исправления Google OAuth, admin hardening, webhook idempotency, API-token enforcement, internal replay protection, complete CSRF/session lifecycle и сокращение Telegram login TTL.

### Проверка HMAC, cache TTL и quota replay - 2026-06-30

Итоговый security-статус: `Заблокировано`

Статус internal HMAC P1: `Закрыто`

Статус production cache/quota contract: `Заблокировано - scanner остаётся доверенным источником скидки`

Подтверждено:

- Internal endpoints работают по HMAC-only. `Authorization: Bearer <INTERNAL_API_SECRET>` не даёт доступ без корректной подписи.
- Обязательны `X-Key-Id`, уникальный `X-Request-Id`, timestamp и signature. Каноническая строка связывает timestamp, key id, request id, HTTP method, path, canonical query и SHA-256 digest сырого тела.
- HMAC старше 5 минут отклоняется. Replay ledger из миграции `0011_api_auth_and_internal_replay.sql` хранит `(key_id, request_id)` 10 минут; повтор подписи возвращает `409 internal_request_replayed`.
- Cache hit принимается только при `cacheStatus=hit`, непустом `cacheCreatedAt`, совпадении `cacheGenerationVersion` с `generationVersion`, возрасте не из будущего и не старше policy TTL.
- Границы cache TTL проверены: 59 минут - hit, 60 минут - hit, 61 минута - miss с полной стоимостью.
- `forceRefresh=true` всегда даёт `bypass` и полную стоимость нового анализа.
- Цена берётся из server-owned `quota_plan_policies`; half-unit хранится целыми credits, без floating-point записи в D1.
- Идемпотентность использует составной ключ `(requestId, ticker)`. Повтор идентичного payload возвращает `own_repeat`/`own_repeat_fundrep` с `chargeUnits=0` и не увеличивает баланс повторно; изменённый payload для того же ключа отклоняется.
- Миграция `0012_access_contract_v11_half_credits.sql` успешно применена к локальной D1.

Блокирующее замечание:

- **P1, ответственный: Яша / владелец scanner contract.** HMAC предотвращает подмену cache-полей внешним клиентом, но не доказывает существование cache entry: scanner сам владеет HMAC-ключом и сам задаёт `cacheStatus`, `cacheCreatedAt`, `cacheGenerationVersion` и `generationVersion`. Он может подписать согласованный свежий `hit` без реального cache entry и получить пониженное списание. Проверки TTL и generation consistency не устраняют этот trust gap.

Требуемое исправление:

- Сделать скидку зависимой от server-verifiable evidence: core-owned cache ledger/receipt, отдельный доверенный cache service с подписанным receipt, либо lookup по cache entry id, который scanner не может создать самостоятельно.
- Альтернатива требует явного управленческого решения считать scanner доверенным billing oracle и принять риск письменно; без такого решения production gate не снимается.
- Добавить Workers-runtime/D1 integration test конкурентных одинаковых запросов. Текущие unit tests подтверждают state machine и последовательный replay, но используют mocked D1.

Проверки:

- `npm run check` - passed.
- `npm test` - passed, 37 tests.
- Добавлены негативные тесты Bearer-обхода, просроченной HMAC, подмены cache metadata, TTL 59/60/61, refresh и повторного списания.

### Финальный security review Core / Scanner - 2026-07-01

Итог: `FAIL`

Отдельные статусы:

- `API token P0`: `Закрыто в коде, deployment заблокирован конфигурацией`.
- `Internal HMAC P1`: `Криптография закрыта, authorization scopes не закрыты`.
- `Core-owned cache receipt`: `Закрыто в Core, не интегрировано в Scanner/dev`.

Подтверждённые контроли:

- `POST /api/analysis/requests` требует API key с server-side scope `analysis:write`; проверяются keyed HMAC hash, active/revoked state, expiry, active user, active subscription и server-owned quota.
- Rate limits атомарно считаются в D1 по API key, user и IP. Legacy SHA-256 API keys не принимаются и требуют rotation.
- Создание ключа использует `API_KEY_HASH_SECRET`, Web Crypto randomness и показывает raw token только в create/rotate response. List API возвращает только prefix.
- Rotation выполняется одной D1 batch: старый key отзывается и новый keyed-hash key создаётся атомарно; тест overlap/retirement для service HMAC keys пройден.
- Core-owned cache ledger из `0013_core_cache_receipts.sql` не доверяет scanner cache hints. Discount появляется только по committed `core_cache_entries` после full-price new/refresh decision.
- Receipt выдаётся только разрешённому full-price решению, действует 15 минут, связывает immutable request/ticker/user/report/generation/language, принимает один digest и использует server time для cache TTL.
- Повтор commit с тем же digest идемпотентен; другой digest или immutable mismatch отклоняется. Conditional transitions и UNIQUE constraints защищают concurrent commit.
- Canonical string Core и Scanner совпадает: `timestamp.keyId.transportRequestId.METHOD.pathname.canonicalQuery.sha256(rawBody)`.
- Негативные тесты подтвердили отказ при неверном secret и подмене timestamp, key ID, request ID, method, path, query или body digest.
- Clock skew ограничен +/-5 минутами; nonce `(keyId, requestId)` хранится 10 минут. Повтор возвращает `409`, Bearer downgrade отсутствует.
- HMAC signature проверяется через Web Crypto-derived fixed-length MAC и constant-time byte loop; прямое строковое сравнение подписи для допуска отсутствует.

Блокеры:

1. **P0 - отсутствуют endpoint scopes для service keys.** Владелец: Яша. Срок: до следующего dev integration smoke. Влияние: любой secret из `INTERNAL_API_SECRETS_JSON`, включая matcher key, может подписать scanner quota/cache endpoint; разделение key IDs не создаёт least privilege. Нужен keyring с secret + scopes и `403 internal_scope_required` до nonce/бизнес-логики.
2. **P0 - Core receipt protocol не интегрирован в Scanner.** Владелец: manager `stock-signal-scanner`. Срок: до dev E2E. Влияние: Scanner читает `cacheReceiptId`, но не вызывает `/api/internal/access/cache/commit`; одновременно он продолжает требовать старые `cacheClaim/cacheClaimSignature`, которых Core не возвращает. Cached/own-repeat flow между реальными сервисами несовместим и fail-closed.
3. **P0 - HMAC production/dev bindings Scanner отсутствуют.** Владелец: Илья/Макс, DevOps. Срок: после исправления scopes, до dev smoke. Secret inventory показывает `CORE_HMAC_SECRET=false` для Scanner dev и production; локальный/remote config не содержит `CORE_HMAC_KEY_ID` и `ACCESS_CHECK_URL`. Реальный Core вызов невозможен.
4. **P0 - Core production keyring не готов.** Владелец: Илья/Макс. Срок: до production approval. Dev имеет `INTERNAL_API_SECRETS_JSON`, production его не имеет и сохраняет legacy `INTERNAL_API_SECRET` без `INTERNAL_API_KEY_ID`, поэтому production internal auth fail-closed.
5. **P0 - API key hashing secret не настроен в окружениях.** Владелец: Илья/Макс. Срок: до API dev smoke. `API_KEY_HASH_SECRET=false` в Core dev и production; API key create/auth возвращает `503`.
6. **P1 - dev Core ещё не содержит schema `0013`.** Владелец: Илья/Макс. Срок: до receipt smoke. Dev `/api/health` не показывает `cache_receipts` и `core_cache_entries`, хотя локально миграции применены.
7. **P2 - API key expiry input не валидируется при create/rotate.** Владелец: Яша. Срок: до public API beta. Невалидная непустая дата даёт `Date.parse=NaN` и фактически не истекающий key. Требуется ISO date validation и запрет past/invalid expiry.
8. **P2 - constant-time helper рано отклоняет строки разной длины.** Владелец: Яша. Срок: до security-ready. Это раскрывает только публичную длину HMAC, не secret, но не полностью соответствует fixed-length-first правилу; следует декодировать ровно 32 bytes и использовать `crypto.subtle.verify`/fixed-length comparison.

Проверки:

- Core: `npm run check` - passed; `npm test` - passed, 49 tests.
- Scanner: `npm run test:worker-contract` - passed, 34 checks.
- Core local D1: pending migrations отсутствуют, включая `0013`.
- Core dev health: HTTP 200; unsigned internal request: HTTP 401.
- Core и Scanner dev/production dry-run bundle: passed.

Production решение:

- Не выдавать `security_review_approved` и не запускать production deployment.
- Повторить review после endpoint scopes, Scanner receipt commit, dev key synchronization и live dev HMAC/cache E2E.

## Oleg - Legal / Compliance Advisor

### Tasks

- [x] Prepare first legal/compliance draft package.
- [x] Prepare risk disclaimer.
- [x] Prepare Terms of Service draft.
- [x] Prepare Privacy Policy draft.
- [x] Prepare Subscription Terms and Refund Policy draft.
- [x] Prepare API Terms of Use draft.
- [x] Prepare required UI disclaimer points.
- [x] Prepare investment recommendation risk review.
- [x] Prepare short production-ready UI texts for Yasha and Glasha.
- [x] Prepare forbidden product phrase list.
- [x] Finalize legal wording for MVP commercial use of API results: display is allowed only with attribution, disclaimers, and plan limits; resale and redistribution are prohibited without a written agreement.
- [ ] Review payment/subscription flow after payment provider is selected.

### Report

Status: `Ready with legal risks`

Completed:

- First legal/compliance draft package is prepared under `docs/legal/`.
- Legal documents are indexed in `docs/documentation-index.md`.
- Investment recommendation risk review is complete as working draft.
- Required disclaimer points are defined for website, Telegram, API docs, and reports.
- Production-ready UI legal microcopy is prepared for Yasha and Glasha.
- Forbidden and high-risk product phrase list is prepared for product, marketing, Telegram, reports, and API docs.
- MVP commercial API-use wording is finalized and added to API Terms, UI legal microcopy, and documentation index.

Not completed:

- Final lawyer review by jurisdiction is not complete.
- Payment-provider-specific review is pending provider decision.
- Payment/subscription flow review remains pending until the provider is selected.
- Final enterprise/API redistribution contract template is not prepared; MVP rule is complete, but custom commercial agreements still need separate counsel review.

Blockers:

- Payment provider not selected.
- Business jurisdiction not confirmed.
- Payment/subscription flow cannot be reviewed completely until provider, checkout flow, trial rules, invoice rules, and refund flow are confirmed.

Risks:

- Product must avoid directive language such as buy/sell/hold, guaranteed profit, best stock for you, and personal recommendation.
- API customers must not be allowed to resell outputs as personalized investment advice.
- MVP API customers may display outputs only with attribution, required disclaimers, and plan limits; resale, redistribution, white-label use, paid signal services, broker integrations, and third-party advisory use require a separate written agreement.
- Legal acceptance checkboxes must not be pre-checked and must store document version and timestamp.
- Footer-only disclaimers are not enough for ticker analysis, checkout, Telegram, reports, or API outputs.

Needs from manager:

- Confirm business jurisdiction.
- Confirm payment provider direction.
- Confirm whether trials will require payment details.
- Confirm refund posture for monthly, annual, API, and custom report purchases.
- Confirm whether any MVP customer needs a separate written commercial API agreement.

Links/files:

- `docs/legal/risk-disclaimer.md`
- `docs/legal/terms-of-service.md`
- `docs/legal/privacy-policy.md`
- `docs/legal/subscription-and-refund-policy.md`
- `docs/legal/api-terms-of-use.md`
- `docs/legal/disclaimer-ui-points.md`
- `docs/legal/investment-recommendation-risk-review.md`
- `docs/legal/ui-legal-microcopy.md`
- `docs/legal/forbidden-product-phrases.md`
- `docs/legal/api-commercial-use-mvp-wording.md`

## telegram_company_matcher_app Manager

### Tasks

- [ ] Confirm service reads or mirrors `contractVersion: "1.0"` contract.
- [ ] Confirm payload fields available from current data model.
- [ ] Identify missing fields for country, language, bot, news, tickers, analysis, delivery.
- [ ] Plan call to `market-signal-ai-bot /api/internal/access` before user-targeted delivery.
- [ ] Confirm idempotency key strategy by `requestId`.
- [ ] Confirm retry and resend-unsent behavior.
- [ ] Перевести все внутренние вызовы к `market-signal-ai-bot` на HMAC-only: отправлять `X-Key-Id`, уникальный `X-Request-Id`, `X-Timestamp` и `X-Signature` по канонической строке из `docs/integration-contract.md`.
- [ ] Удалить Bearer-вызовы к внутренним endpoint-ам и использовать отдельный секрет matcher из `INTERNAL_API_SECRETS_JSON`.
- [ ] Добавить тесты успешной подписи, неверной подписи, изменённого тела/query и повторного `X-Request-Id`; при отказе авторизации не помечать доставку как успешную.
- [ ] Согласовать с Максом одновременное включение нового ключа в dev, проверить интеграцию и только затем готовить prod.
- [x] Получить подтверждение key ID: dev `matcher-dev-v1`; production `matcher-prod-v1` зарезервирован, но пока не активен.
- [x] Получить контракт `POST /api/internal/deliver` версии `1.0` из `docs/integration-contract.md`.
- [ ] После реализации Core подключить dev delivery endpoint и передать signed integration report по шаблону handoff-файла.

### Report

Status: `Ready with contract gaps`

Completed:

- Confirmed current `telegram_company_matcher_app` has the core delivery orchestrator flow:
  - Telegram news scan;
  - company/ticker match;
  - Stock Scanner analysis request;
  - access check before user/channel delivery;
  - delegated delivery through `market-signal-ai-bot`;
  - delivery status and resend/retry tracking.
- Confirmed stable IDs are planned/wired through migration `cloudflare/migrations/0008_stable_ids_access_delivery.sql`:
  - `countries.stable_id`;
  - `telegram_sources.stable_id`;
  - `news_items.stable_id`;
  - `news_items.request_id`;
  - `outbound_events.request_id`;
  - `outbound_events.reason`.
- Confirmed available payload sections for Stock Scanner:
  - `requestId`;
  - `source`;
  - `country`;
  - `bot`;
  - `news`;
  - `tickers`;
  - `analysis`;
  - `delivery`.
- Confirmed current data model can provide most required fields:
  - country: code, market code, name, timezone, language code, stable id;
  - bot: bot id, username, display name, chat id, token secret name;
  - news: request id, source, message id, message date, original text/summary;
  - tickers: ticker, company name, matched name/type, confidence;
  - delivery: outbound event status, attempts, reason, error, response, timestamps.
- Confirmed scanner-side contract details from `stock-signal-scanner Manager` report:
  - `contractVersion: "1.0"` is accepted and required by scanner validation;
  - `risk`: positive number, percent risk per trade;
  - `anchorBars`: positive integer candle count;
  - `strategies`: canonical list only: `trend`, `breakout`, `volume_avwap`, `momentum`.
- Confirmed current matcher `analysis` payload is directionally aligned with scanner shape:
  - `risk` is sent as `Number(country.stock_scanner_risk || 1)`;
  - `anchorBars` is sent as `120`;
  - strategies are sent as `trend`, `momentum`, `breakout`, `volume_avwap`.
- Confirmed access check is implemented before user-targeted delivery:
  - default endpoint: `${MARKET_SIGNAL_AI_BOT_URL}/api/internal/access`;
  - override: `ACCESS_CHECK_URL`;
  - query includes `telegramUserId`, `country`, and `language`;
  - request includes `x-request-id`;
  - `allowed=false` blocks delivery and records `access_denied`/reason.
- Confirmed user/channel delivery is delegated to `market-signal-ai-bot`:
  - endpoint: `${MARKET_SIGNAL_AI_BOT_URL}/api/internal/deliver`;
  - no local subscription business logic is kept in matcher.
- Confirmed idempotency strategy:
  - `news_items` is unique by `source + message_id + ticker`;
  - `outbound_events` is unique by `news_item_id + event_type`;
  - `requestId` is generated by country/date/source/message id and is reused on retry.
- Confirmed retry/resend behavior:
  - Stock Scanner delivery uses `pending`, `sending`, `failed`, `sent`, `dead_letter`;
  - Telegram delivery uses `telegram_delivery` events;
  - resend skips `sent`, `access_denied`, and `dead_letter`;
  - retry is gated by attempts, max attempts, and `next_retry_at`.
- Confirmed manager/admin visibility:
  - `/api/delivery`;
  - `/api/delivery-log`;
  - delivery filters for `pending`, `failed`, `skipped`, `access_denied`, `sent`, `dead_letter`.
- Verified locally:
  - `npx.cmd tsc --noEmit --pretty false` passes;
  - `npm test` passes with 9 tests.

Not completed:

- `contractVersion: "1.0"` is not currently included in the Stock Scanner JSON payload.
- The Stock Scanner payload is assembled inline in `requestStockScannerAnalysisStrict`; it is not yet isolated as a typed `buildStockScannerPayloadV1` builder.
- `news.language` is currently sent as `null`; it should be aligned with country/source language once the contract requires it.
- The current `news.id` still embeds source/message-derived technical shape for Stock Scanner id; this is internal-to-scanner, but should be reviewed against the new rule that user-facing messages must not expose monitored source.
- Access/retry coverage is mostly contract/string coverage, not full behavioral mock e2e.

Blockers:

- `contractVersion: "1.0"` must be added before the contract can be considered locked.
- Production deploy remains blocked on production D1/secrets/migrations/smoke readiness in `telegram_company_matcher_app`.

Risks:

- Since `stock-signal-scanner` validates `contractVersion`, current matcher payload may be rejected until `contractVersion: "1.0"` is added.
- Strategy names are valid, but matcher should still centralize them in a typed payload builder to avoid drift.
- Current tests may miss behavioral regressions in access allowed/denied and retry idempotency because they are not full mock e2e.
- User-facing delivery must stay editorially rewritten and must not reveal source/channel/message metadata.

Needs from manager:

- Approve a matcher patch to add `contractVersion: "1.0"` to every Stock Scanner request.
- Confirm whether `news.language` should be required in v0.1 or can remain `null` until the next contract version.
- Confirm initial countries/languages remain Israel Hebrew and US Russian.
- Confirm whether user-facing delivery text should be generated by matcher or by `market-signal-ai-bot /api/internal/deliver`.

Links/files:

- `C:/Users/fnemo/Desktop/Trade/telegram_company_matcher_app/cloudflare/src/worker.ts`
- `C:/Users/fnemo/Desktop/Trade/telegram_company_matcher_app/cloudflare/src/ui.ts`
- `C:/Users/fnemo/Desktop/Trade/telegram_company_matcher_app/cloudflare/migrations/0008_stable_ids_access_delivery.sql`
- `C:/Users/fnemo/Desktop/Trade/telegram_company_matcher_app/tests/pipeline-contract.test.mjs`
- `C:/Users/fnemo/Desktop/Trade/telegram_company_matcher_app/tests/e2e-pipeline-scenario.test.mjs`

## stock-signal-scanner Manager

### Tasks

- [x] Confirm final analysis request shape:
  - `risk`
  - `anchorBars`
  - `strategies`
- [x] Confirm scanner can accept `contractVersion: "1.0"`.
- [x] Plan runtime validation for required fields.
- [x] Plan structured response format.
- [x] Plan idempotency by `requestId`.
- [x] Confirm no user subscription logic will live in scanner.
- [ ] Подключить `POST /api/internal/access/check` перед платным или пользовательским анализом и сохранять поведение fail-closed при недоступности core.
- [ ] Подписывать scanner-to-core запросы по HMAC-only с `X-Key-Id`, уникальным `X-Request-Id`, `X-Timestamp`, digest тела и отдельным scanner-секретом.
- [ ] Удалить Bearer-вызовы внутренних endpoint-ов и добавить тесты успешной подписи, подмены тела, повтора request id, отказа доступа и недоступности core.
- [ ] Согласовать с Максом синхронное включение ключа в dev; prod не обновлять до одобрения Романа и успешной интеграционной проверки.
- [ ] Исправить требование `Authorization: Bearer <INTERNAL_API_SECRET>`: scanner должен использовать утверждённую HMAC-only схему, поскольку Bearer-обход закрыт как уязвимость P1.
- [ ] Уточнить, как scanner передаёт достоверный cache hit/miss до решения core; одного `generationVersion` недостаточно для выбора `cached_*` против `new_*`.
- [ ] Подтвердить, поддерживает ли один `requestId` несколько тикеров; это определяет схему идемпотентности `requestId + ticker` и формат ответа.
- [ ] После фиксации контракта обновить scanner-клиент, контрактные тесты и передать dev-отчёт до production gate.
- [ ] После успешного нового/refresh анализа вызвать `POST /api/internal/access/cache/commit` с Core-issued receipt и digest результата; при неуспешном анализе receipt не подтверждать.
- [ ] Не считать локальный cache hit основанием для скидки: использовать только решение и `reportSource` из ответа Core.

### Report

Status: `Completed with production-gate dependency`

Completed:

- Confirmed final scanner analysis request shape:
  - `risk`: positive number, percent risk per trade.
  - `anchorBars`: positive integer candle count.
  - `strategies`: canonical list only: `trend`, `breakout`, `volume_avwap`, `momentum`.
- Confirmed `stock-signal-scanner` accepts `contractVersion: "1.0"` on `POST /api/external/analyze`.
- Confirmed runtime validation is implemented for required contract fields: `contractVersion`, `requestId`, `tickers`, `country`, `news`, `analysis`, and `delivery`.
- Confirmed invalid contract payload returns `400` with structured `rejected` response.
- Confirmed structured response format includes `contractVersion`, `requestId`, `status`, `report`, `telegram`, and `errors`.
- Confirmed idempotency by `requestId` is implemented through `contract_results`.
- Confirmed scanner does not own user subscription logic.
- Confirmed scanner treats SaaS access and tariff limits as external responsibility of `market-signal-ai-bot`.
- Confirmed service token is accepted from headers only for `POST /api/external/analyze`.
- Confirmed Telegram tokens are rejected from payload.
- Confirmed user-facing Telegram report no longer exposes internal `requestId` and no longer mixes Russian and Hebrew in the analysis explanation.
- Verified locally:
  - `node --check cloudflare/worker.js`
  - `npm.cmd run test:worker-contract`

Not completed:

- Production gate for `stock-signal-scanner` is not complete yet.
- Integration call from scanner to `market-signal-ai-bot` for quota/tariff limit check is planned but not implemented in scanner yet.
- Основной endpoint `POST /api/internal/access/check` реализован Яшей и передан Роману на проверку безопасности; scanner ещё должен подключить и проверить этот вызов.

Blockers:

- Production deployment must wait for scanner production gate: production secrets, production D1 backup, production migration, production Worker deploy, production Telegram webhook reset with `secret_token`, and production health checks.
- Для применения тарифов и квот `stock-signal-scanner` должен подключить реализованный `POST /api/internal/access/check` после одобрения Романа.

Risks:

- If scanner implements subscription or tariff logic internally, ownership boundaries will become duplicated and inconsistent. This must stay in `market-signal-ai-bot`.
- If quota check is added without clear fail-closed behavior, paid-access enforcement may be bypassed during service outages.
- Production release before webhook secret and service token checks are verified can reopen P0 security issues.

Needs from manager:

- Передать менеджеру scanner итоговое заключение Романа и согласованный секрет и заголовки внутренней авторизации.
- Подтвердить или заменить лимиты MVP: `regular=1`, `fundrep=5`, `trial=25`, `starter=100`, `pro=1000`; повтор запроса с тем же `requestId` не списывает единицы повторно.
- После одобрения Романа подключить вызов из scanner в основной сервис и подтвердить отказ в анализе при недоступности `market-signal-ai-bot`.
- Confirm whether scanner should support only Russian Telegram reports for v0.1/v1.1 or whether language should come from upstream contract.

Links/files:

- `C:\Users\fnemo\Desktop\Trade\stock-signal-scanner\cloudflare\worker.js`
- `C:\Users\fnemo\Desktop\Trade\stock-signal-scanner\cloudflare\schema.sql`
- `C:\Users\fnemo\Desktop\Trade\stock-signal-scanner\tests\worker-contract-smoke.mjs`
- `C:\Users\fnemo\Desktop\Trade\stock-signal-scanner\docs\grisha-analysis-api-tasks.md`
- `C:\Users\fnemo\Desktop\Trade\stock-signal-scanner\docs\ilya-production-gate-task.md`
## Project Manager / Documentation Owner

### Tasks

- [x] Create SaaS architecture documentation.
- [x] Create team roles documentation.
- [x] Create QA, DevOps, Legal plans.
- [x] Create production checklist.
- [x] Create version roadmap.
- [x] Create version task-board process.
- [x] Create first version task board.
- [x] Review the final Core/Scanner reports from Roman and Lena after the main manager signal.
- [ ] Prepare final v0.1 management report.

### Report

Status: `In progress`

Completed:

- Documentation process and v0.1 task-board created.
- Oleg's legal package accepted into the documentation flow.

Not completed:

- Final v0.1 management report is pending specialist reports and main manager signal.

Blockers:

- Waiting for specialist reports.

Risks:

- Contract details between matcher and scanner are still open.

Needs from manager:

- Confirm payment provider direction.
- Confirm business jurisdiction.
- Confirm first supported countries/languages.

Links/files:

- `docs/version-task-process.md`
- `docs/versions/v0.1-task-board.md`

### Manager gate review: Core / Scanner, 2026-07-02

Status: `Production blocked`

Manager verification:

- Roman completed the assigned security review. His verdict is `FAIL`, not a production approval.
- Lena completed the assigned integration QA review. Her verdict is `FAIL` for the deployed multi-service integration; the local Core `1.1 + 0013` runtime is `PASS WITH RISKS`.
- Independent local verification passed: TypeScript and `49/49` automated tests.
- Passing local tests confirms the Core implementation baseline, but does not replace a deployed Core + Scanner integration test.
- The production release remains blocked until both Roman and Lena issue a new approving verdict after the fixes and live dev regression.

Blocking assignments:

1. Yasha: implement endpoint-level service scopes. Scanner keys may use only `scanner:access` and `scanner:cache`; matcher keys may use only matcher routes. Return `403 internal_scope_required` before nonce consumption and business logic. Add cross-service negative tests.
2. Yasha: validate `expiresAt` on API-key create and rotate as a valid future ISO timestamp. Reject invalid and past values with `400`.
3. Yasha: replace the early length rejection in the secret comparison helper with a fixed-length Web Crypto comparison that follows the current Cloudflare recommendation.
4. Manager `stock-signal-scanner`: integrate HMAC-only calls to `/api/internal/access/check` and `/api/internal/access/cache/commit`, remove the obsolete `cacheClaim/cacheClaimSignature` dependency, and keep analysis fail-closed on Core errors or denial.
5. Max: `Completed 2026-07-02` - deployed the current Core Worker to dev, configured a separate Scanner dev key and `API_KEY_HASH_SECRET`, completed signed smoke and left production unchanged.
6. Lena: repeat the deployed dev Core + Scanner regression for access denial, quota denial, regular/FundRep, new/cached/refresh/repeat, receipt/commit, timeout, malformed response, `401/403/409/5xx`, and verify that no analysis runs when Core is unavailable.
7. Roman: repeat the security review after scopes, fixed-length comparison, Scanner receipt integration and dev key synchronization. Issue `PASS`, `PASS WITH RISKS`, or `FAIL`.

Release condition:

- Production deployment is allowed only after Lena and Roman both provide an approving verdict and Max records the successful dev smoke report without exposing secrets.

## Blockers

- Security verdict from Roman is `FAIL`: endpoint-level service scopes and remaining security corrections are not closed.
- Integration verdict from Lena is `FAIL`: deployed dev Core + Scanner receipt/commit and fail-closed scenarios are not complete.
- Scanner has not completed the live HMAC-only access and cache-commit integration with deployed dev Core.
- Core dev is deployed and basic HMAC smoke passes; live Scanner receipt/commit and service-scope regression are still required. Production remains unchanged.
- Matcher delivery endpoint and full matcher-to-delivery regression remain separate open release work.

## Decisions Needed From Main Manager

- Confirm target MVP launch date: 2026-08-03 or adjust.
- No new decision is required for the Core/Scanner gate. The already approved fail-closed security policy applies.
- Previously confirmed: Paddle first, Delaware C-Corp, Israel/Hebrew and US/Russian, and no production release before Roman's security approval.

## Final Project Manager Summary

Status: `Production blocked by security and integration gates`

Final report:

- `docs/versions/v0.1-management-report.md`

Summary:

- Most v0.1 foundation work is complete.
- Core contract `1.1`, quota accounting and Core-owned cache receipt/commit are implemented locally; TypeScript and `49/49` tests pass.
- Roman completed the security review with verdict `FAIL` until service scopes, Scanner integration, environment secrets and remaining security corrections are closed.
- Lena completed integration QA with verdict `FAIL` for deployed multi-service integration; local Core runtime is `PASS WITH RISKS`.
- Glasha completed UX map and wireframe scope.
- Max prepared isolated dev infrastructure and migrations, but must deploy the current approved Worker and synchronize dev secrets after fixes.
- Oleg completed legal draft package and reports ready with legal risks.
- `stock-signal-scanner` must complete live HMAC-only access and cache receipt commit integration.
- `telegram_company_matcher_app` delivery integration remains open and must use its own scoped key.

Main manager decisions still required:

- Confirm or adjust the target MVP launch date `2026-08-03` after the next security and QA verdicts.

Recommended follow-ups:

- Yasha closes service scopes, API-key expiry validation and fixed-length secret comparison.
- The Scanner manager integrates access check and receipt commit in dev.
- Max synchronizes dev-only deployment and secrets after code approval.
- Lena and Roman repeat their independent gates; production remains blocked until both approve.

### Max report: CORE-DEV-DEPLOY-ACCESS-CHECK-TRAILING-SLASH, 2026-07-08

Environment: `dev`, Worker `market-signal-ai-bot-dev`, URL `https://market-signal-ai-bot-dev.fnemoy.workers.dev`

Core dev version: `c5480694-c028-422c-9b8e-8aeab17f3696`

Deploy status: `PASS`; deployed only `env dev` after `npm.cmd run check` and `npm.cmd test` passed (`58/58`).

Health status: `PASS`; `GET /api/health` returned `200`.

Exact path status: `PASS`; unsigned `POST /api/internal/access/check` returned `401 {"error":"internal_key_and_request_id_required"}`, not `404`.

Trailing slash path status: `PASS`; unsigned `POST /api/internal/access/check/` returned `401 {"error":"internal_key_and_request_id_required"}`, not `404`.

Scanner key id present: `yes`; active dev config exposes safe key id `scanner-dev-v2`, and dev secrets include `INTERNAL_API_SECRETS_JSON`, `INTERNAL_API_SECRET`, and `API_KEY_HASH_SECRET`. Secret values were not read or printed.

Production changed: `no`; production deployment remains `542a4d29-49da-4632-a67a-48c6f76df0fa`, version `967192f3-12cc-4176-8dc5-ceb0b0d21ae7`.

Live tail window: `2026-07-08 20:07:27-20:22:27 Asia/Tbilisi`; tail started for Core dev and writes to `.wrangler/live-tail-core-dev-20260708-200727.jsonl` with stderr in `.wrangler/live-tail-core-dev-20260708-200727.err.log`.

Result: `PASS`

Blockers: none for Core dev trailing-slash alias. Scanner team can repeat signed E2E against `https://market-signal-ai-bot-dev.fnemoy.workers.dev` with active key id `scanner-dev-v2`; both `/api/internal/access/check` and `/api/internal/access/check/` are live. HMAC secret, signatures, and raw auth headers were not disclosed.

### Max report: website dev HMAC key activation, 2026-07-08

Environment: `dev`, Worker `market-signal-ai-bot-dev`

Core dev version: `392e4553-f061-4760-9bca-21c1aeb30703` after dev secret update.

Deploy status: `PASS`; code was not redeployed, dev Worker secret version updated by Cloudflare.

Secret activation: `PASS`; created dev secret binding `INTERNAL_API_SECRET_WEBSITE_DEV_V1` and kept local handoff file at `.wrangler/handoff/INTERNAL_API_SECRET_WEBSITE_DEV_V1.txt`. Secret value is not written in docs or logs.

Scopes status: `PASS`; updated `INTERNAL_API_SCOPES_JSON` with `website-dev-v1: ["website:subscriptions"]` while preserving scanner/matcher scopes for `scanner-dev-v2`, `scanner-dev-v1`, and `matcher-dev-v1`.

Unsigned website route smoke: `PASS`; `POST /api/internal/subscriptions` without HMAC returned `401 {"error":"internal_key_and_request_id_required"}`.

Signed website route smoke: `PASS`; `POST /api/internal/subscriptions` signed with key id `website-dev-v1` returned `200` and a subscription contract response for dev fixture user `dev_test_scanner_user`.

Scanner/matcher website-route smoke: `BLOCKED`; real raw scanner/matcher HMAC secrets are not readable from Cloudflare and are not stored locally. I did not rotate scanner/matcher dev secrets because that could break the active dev E2E. Expected behavior is covered by endpoint scopes and should return `403 internal_scope_required` for valid scanner/matcher signatures against `/api/internal/subscriptions`.

Production changed: `no`; production remains deployment `542a4d29-49da-4632-a67a-48c6f76df0fa`, version `967192f3-12cc-4176-8dc5-ceb0b0d21ae7`.

Result: `PASS with one verification blocker`

Blockers: to complete the exact live `matcher/scanner keys -> website route -> 403` smoke, owners of scanner/matcher must provide a signed test request or approve a coordinated dev-only rotation/test window for their HMAC secrets. No HMAC secret, signature, or raw auth header was disclosed.
