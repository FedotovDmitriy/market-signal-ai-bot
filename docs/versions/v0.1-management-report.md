# v0.1 Management Report - SaaS Core And Contracts

Date: 2026-06-29.

Version: `v0.1`

Status: `Заблокировано до закрытия security-gate`

## Executive Summary

The v0.1 foundation is mostly complete. The team aligned the SaaS product model, core account/access responsibilities, QA/DevOps/Legal gates, and most of the integration contract.

The version should not be fully closed until the main manager confirms several product and operational decisions:

- Payment provider direction.
- Business jurisdiction.
- Initial countries/languages.
- Website authentication model.
- Commercial API usage rules.
- Final matcher patch to add `contractVersion: "1.0"` to scanner requests.

## What Was Completed

### Yasha - Developer

Status: `В работе - устранение P0/P1`

Completed:

- Confirmed current `market-signal-ai-bot` endpoints against README and docs.
- Confirmed SaaS account routes:
  - `/api/me`
  - `/api/me/profile`
  - `/api/me/subscription`
  - `/api/me/api-keys`
  - `/api/me/analysis-history`
- Confirmed API key and analysis history tables are wired to endpoints.
- Confirmed `/api/internal/access` behavior for active, expired, and missing subscriptions.
- Fixed README/API documentation mismatch.
- Documented `subscription_period_expired`.
- Added test coverage.
- Verified checks pass with 16 tests.
- Реализован scanner-to-core endpoint `POST /api/internal/access/check`, квоты, защита от двойного списания и миграция `0008`; проверено 24 тестами.
- Устранён захват аккаунта через неподтверждённый email, добавлены `email_verified_at` и `pending_email`, миграции `0009`/`0010`; проверено 27 тестами.
- Роман повторно проверил исправление и закрыл email-P0.
- Реализована авторизация `POST /api/analysis/requests` через API-ключи со scope, подпиской, квотой и лимитами; добавлены keyed hash и атомарная ротация ключей.
- Внутренние endpoint-ы переведены на HMAC-only с отдельными key id, request id и защитой от повторного воспроизведения в миграции `0011`; TypeScript и 33 теста проходят.

Open risk:

- Общий security-gate остаётся заблокированным из-за остальных P0/P1.

### Glasha - Designer

Status: `Ready for product review`

Completed:

- Created `docs/saas-ux-v0.1.md`.
- Prepared SaaS dashboard UX map.
- Prepared wireframe list for core SaaS screens.
- Added legal/disclaimer placement points.
- Defined empty/loading/error/blocked/expired/rate-limited states.
- Defined Telegram WebApp versus full website responsibilities.

Open risk:

- Pixel-perfect designs are not done.
- Final UX copy depends on auth method, payment provider, API commercial policy, and launch countries/languages.

### Lena - QA Engineer

Status: `Ready with risks`

Completed:

- Prepared v0.1 regression checklist.
- Expanded automated checks for account, Telegram init data, subscription access, internal auth failure, CORS, rate limits, and legal/disclaimer criteria.
- Verified `npm.cmd run check` and `npm.cmd test` pass with 16 tests.

Open risk:

- No full staging regression yet because staging/prod Cloudflare resources are not provisioned.
- Payment flow cannot be QA-approved until provider is selected.
- Real Telegram webhook behavior still needs staging verification.

### Max - DevOps / Cloudflare Engineer

Status: `Ready with infrastructure risks`

Completed:

- Confirmed dev/prod environment plan for the current phase; staging is deferred.
- Confirmed per-environment secrets.
- Confirmed D1 migration and backup/export flow.
- Confirmed Queue/DLQ needs.
- Confirmed health-check requirements.
- Prepared first runbook list.

Open risk:

- Actual Cloudflare dev/prod resources are not created or wired yet.
- `wrangler.jsonc` does not yet define separate `env.dev`, `env.staging`, and `env.production`.
- CI/CD does not yet run tests or require manual production approval.

### Oleg - Legal / Compliance Advisor

Status: `Ready with legal risks`

Completed:

- Prepared first legal/compliance draft package.
- Prepared risk disclaimer, Terms, Privacy Policy, Subscription/Refund Policy, API Terms, UI disclaimer points, investment recommendation risk review, UI legal microcopy, and forbidden product phrases.

Open risk:

- Final lawyer review by jurisdiction is not complete.
- Payment-provider-specific legal review is pending.
- Business jurisdiction is not confirmed.

### telegram_company_matcher_app Manager

Status: `Ready with contract gaps`

Completed:

- Confirmed matcher has the core delivery orchestrator flow.
- Confirmed stable IDs and delivery tracking plan.
- Confirmed most scanner payload sections are available.
- Confirmed access check is implemented before user-targeted delivery.
- Confirmed delivery is delegated to `market-signal-ai-bot`.
- Confirmed idempotency, retry/resend behavior, and admin visibility.
- Verified local TypeScript and tests in that service.

Open risk:

- `contractVersion: "1.0"` is not yet included in Stock Scanner JSON payload.
- Payload builder is inline and should be isolated as typed `buildStockScannerPayloadV1`.
- `news.language` is currently `null`.

### stock-signal-scanner Manager

Status: `Completed with production-gate dependency`

Completed:

- Confirmed final analysis request shape:
  - `risk`: positive number.
  - `anchorBars`: positive integer.
  - `strategies`: `trend`, `breakout`, `volume_avwap`, `momentum`.
- Confirmed scanner accepts `contractVersion: "1.0"`.
- Confirmed runtime validation.
- Confirmed structured rejected response for invalid payload.
- Confirmed structured response format.
- Confirmed idempotency by `requestId`.
- Confirmed scanner does not own subscription logic.

Open risk:

- Production gate is not complete.
- Scanner-to-core quota/tariff endpoint реализован в основном сервисе, но scanner ещё должен подключить его после закрытия требований Романа к внутренней авторизации.

## Main Blockers

1. Paddle approval is not complete; management decision is to check Paddle first as Merchant of Record.
2. Business jurisdiction direction is confirmed: Delaware C-Corp.
3. Website authentication model needs final technical confirmation: Telegram login, mandatory email, Google login, protected website sessions.
4. Matcher must add `contractVersion: "1.0"` before contract lock.
5. Core endpoint `POST /api/internal/access/check` и HMAC-only защита реализованы; требуются проверка Романа, отдельные секреты dev/prod и синхронное обновление matcher/scanner.
6. Cloudflare dev готов: отдельные Worker/D1/HMAC key IDs, миграции `0001`-`0012`, health и fail-closed smoke проверены. Production остаётся на старой версии и заблокирован до `0013`, HMAC-интеграции scanner/matcher и общего go/no-go. Staging отложен.
7. Security status before v1.0 is `Заблокировано`: email-P0 закрыт; API-ключи и HMAC исправлены Яшей, но ожидают проверки Романа. Google OAuth, защита админки, идемпотентность платёжных уведомлений, полная CSRF/session-защита и короткий Telegram login TTL ещё не завершены.
8. Natalia's local lawyer/accountant review is complete with status `Готово с рисками`; Sergey's MoR accounting setup is complete with status `Готово с рисками`; paid launch still requires Paddle approval, external CPA/bookkeeper selection, founder/local tax residency review, securities perimeter review, and CPA approval of the accounting workflow.

9. Причина production `404` подтверждена: активна старая версия Worker без `/api/internal/access/check`. Production D1 сохранена в проверенный export; Worker, миграции и секреты не изменялись. Deploy остаётся заблокированным.
10. Входящее требование scanner меняет стоимость и семантику квоты: дробные списания за cache, TTL 60 минут, новые значения решения и `generationVersion`. Нужны решение главного менеджера и явный cache signal от scanner.
11. Роман закрыл HMAC-only, TTL и replay, но заблокировал cache discount: подписанный scanner cache hint не доказывает существование записи. Принято исправление через Core-issued receipt и core-owned committed cache ledger; требуются миграция `0013`, повторная проверка Романа и QA.
12. Лена подтвердила Core `1.1` на уровне автоматических тестов и dev smoke: TypeScript и `42/42` теста проходят. Сквозная dev-регрессия заблокирована до dev test fixture, deployed HMAC scanner/matcher и реализации receipt/commit в `0013`.
13. Для matcher подтверждены key IDs `matcher-dev-v1` и зарезервированный `matcher-prod-v1`, а также контракт `POST /api/internal/deliver` версии `1.0`. Endpoint пока отсутствует в Core и назначен Яше как P0; до реализации и live dev delivery matcher-интеграция заблокирована.
14. Макс применил миграцию `0013` и безопасную QA-фикстуру только в dev D1; production read-only проверка показала `0` fixture rows. Текущие TypeScript и `45/45` тестов проходят, но Worker с `0013` ещё не развёрнут, поэтому live receipt/commit gate остаётся открытым.
15. Яша завершил Core-owned receipt/commit реализацию `0013`. Перед security review добавлен обязательный endpoint-scope gate для HMAC key IDs, чтобы matcher и scanner не могли вызывать endpoint-ы друг друга только на основании валидной подписи.

## Decisions Needed From Main Manager

### Required Now

1. Confirm final website auth implementation details.
   - Product direction: Telegram login + mandatory email + Google login + protected website sessions.
   - Still needed: exact email confirmation flow and Google OAuth credentials/redirect URLs.

2. Start Paddle approval as first Merchant of Record provider.
   - Owner: Marina.
   - Support: Oleg for legal wording, Natalia for KYC/tax/accounting questions, Yasha/Max for technical webhook/domain inputs.
   - Lemon Squeezy remains backup.
   - Direct Stripe is not first launch path unless tax/invoice/VAT/sales-tax operations are ready.

3. Proceed with Delaware C-Corp preparation.
   - Owner: Natalia.
   - Support: Marina for business/payment requirements, Oleg for legal document alignment.
   - Still needed before paid launch: formation provider / registered agent, founder/local tax residency review, CPA setup.

4. Select external CPA/bookkeeper and approve Paddle MoR accounting workflow.
   - Setup owner: Sergey.
   - External approval needed: CPA/bookkeeper.
   - Support: Natalia for tax/accounting requirements, Marina for Paddle reports and payout structure.

### Required Before Production

5. Confirm Cloudflare account/project naming convention for dev/prod.
6. Confirm production domains/subdomains.
7. Confirm who approves production deploys.
8. Confirm quota model for `POST /api/internal/access/check`:
   - one unit per ticker;
   - higher cost for `FundRep`;
   - whether cached/idempotent repeats consume quota.
9. Confirm fail-closed policy if `market-signal-ai-bot` is unavailable.

10. Confirm security-gate policy for v1.0.
   - Recommendation from Roman: do not launch publicly until status is `Безопасность готова`.
   - Temporary `Готово с рисками безопасности` should require explicit management acceptance and documented compensating controls.

11. Confirm remaining launch-prerequisite vendors/owners.
   - Formation provider / registered agent for Delaware C-Corp.
   - CPA/bookkeeper for final approval of MoR accounting setup.
   - Local tax adviser for founder/local tax residency review.
   - Securities counsel or qualified legal reviewer for investment-adviser perimeter review.

## Specialist Follow-Up Recommendations

### Separate follow-up with telegram_company_matcher_app Manager

Reason:

- The only direct contract blocker is in matcher.
- Must add `contractVersion: "1.0"`.
- Should isolate typed payload builder.
- Must decide `news.language` behavior.

Recommended task:

```text
Patch matcher to include contractVersion: "1.0" in every Stock Scanner request and extract request construction into buildStockScannerPayloadV1.
```

### Separate follow-up with Max

Reason:

- Infrastructure is not production-ready yet.
- Need dev/prod Cloudflare split.
- Need CI/CD test gate and manual production approval.

Recommended task:

```text
Create Cloudflare environment plan and prepare wrangler dev/prod config proposal.
```

### Separate follow-up with Oleg

Reason:

- Legal package is good, and the MVP API commercial-use wording is complete.
- Remaining Oleg task depends on payment provider, jurisdiction, checkout flow, trial rules, invoice rules, and refund flow.

Recommended task:

```text
Review payment/subscription flow after payment provider is selected and align Terms, Subscription Terms, Refund Policy, checkout wording, and customer-facing billing notices.
```

### Separate follow-up with Glasha and Yasha together

Reason:

- Legal disclaimers and first-use acknowledgments need UI + implementation alignment.

Recommended task:

```text
Define the exact disclaimer UX components and persisted acceptance fields for signup, checkout, first ticker analysis, Telegram, API docs, and reports.
```

## Version Close Recommendation

Do not mark v0.1 as fully closed yet.

Recommended status:

```text
Ready with management decisions required
```

v0.1 can be closed after:

1. Main manager confirms the required product decisions.
2. Matcher adds `contractVersion: "1.0"`.
3. Version task board is updated with final decisions.

## Proposed Next Version Focus

`v0.2 - Integrated Telegram News To Ticker Analysis Flow`

Primary goals:

- Matcher sends valid `contractVersion: "1.0"` scanner payload.
- Scanner validates and returns structured response.
- Matcher checks core access before delivery.
- Failed scanner/access requests do not create false delivery success.
- Request idempotency works end to end.
